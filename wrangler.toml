name = "rizik-web"
compatibility_date = "2024-04-01"
pages_build_output_dir = "build/web"

# 🧠 Service Bindings (Connect Pages to Backend Worker)
[[services]]
binding = "BACKEND"
service = "rizik-backend"

# ❌ No Migrations in Pages (Handled by rizik-backend)
