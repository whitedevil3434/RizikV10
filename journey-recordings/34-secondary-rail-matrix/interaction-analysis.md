# Secondary World rail matrix

Viewport: 1440×900, live browser device metrics. Dataset: `demo=goods_handoff`, starting from `slice=fixture:goods_handoff:execution&focus=evidence:1`.

Live results:

- Search → same demo/slice/focus → close restores the exact World URL.
- Relations → same demo/slice/focus → close restores the exact World URL.
- Contexts → same demo/slice/focus → close restores the exact World URL.
- Profile → same demo/slice/focus → close restores the exact World URL.
- Settings → same demo/slice/focus → close restores the exact World URL.

All five remain `WORLD · FIXTURE PREVIEW` overlays above one persistent corkboard. None reintroduces a dashboard sidebar, freeform editor canvas or a second material surface.
