# Canonical queue-to-evidence interaction audit

Viewport: 1440×900, live browser device metrics. Dataset: `demo=goods_handoff`.

Timeline:

- `00-world`: the goods-handoff World shows the execution chain and visible attention signals.
- `01-queues`: Queues reports `4 actions · 7 visible records` across the seven participant queues.
- `02-evidence-inbox`: Evidence Inbox clearly identifies `Condition record` as waiting reality.
- `03-expanded-evidence`: the item exposes why, authority, required evidence, privacy and a concrete next safe action.
- `04-capture-intent`: `Capture evidence` opens `Add evidence` above the same World; the URL retains `demo`, execution slice and typed evidence focus.
- `05-recovered-world`: closing the intent removes only `detail/capture` and keeps `slice=fixture:goods_handoff:execution` plus `focus=evidence:1`.

This is the intended corkboard-first boundary: the board finds the record, the queue explains responsibility, and the capture intent makes the authenticated action boundary explicit without pretending a fixture changed canonical data.
