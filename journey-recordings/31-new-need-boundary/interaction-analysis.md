# “+ New” Need-boundary audit

Viewport: 1440×900, live browser device metrics. Dataset: `demo=goods_handoff`.

Timeline:

- `00-world`: the populated World is the starting surface.
- `01-describe-need`: `+ New` opens `Describe a Need` above the same World and labels it `INTENT → NEED · FIXTURE PREVIEW`.
- `02-meaning-preview`: natural language produces a reviewable interpretation (“Resource coordination”) without creating a record.

The preview explicitly says the authenticated workflow must establish context and participants before sharing. No freeform board arrangement, canonical mutation or share link is implied.
