# Copilot advisory interaction audit

Viewport: 1440×900, live browser device metrics.

Timeline:

- `00-world`: Copilot is visible as an advisory card, separate from the primary action hierarchy.
- `01-suggestions`: opening Suggestions exposes explainable suggestions and the human-approval boundary.
- `02-ai-focus`: `Inspect AI responsibility` focuses the typed AI Actor in its personal slice and preserves the execution journey.
- `03-actor-record`: `Review actor` opens the canonical actor record above the same World with `fixture`, `slice` and typed `focus` retained.

No editor-style mutation or freeform arrangement appeared in this path. The action remains inspect-and-review, not automatic approval.
