# Mobile “+ New” Need-boundary audit

Viewport: 390×844, live browser device metrics. Dataset: `demo=goods_handoff`.

`+ New → Describe a Need → natural-language interpretation → close` fits the mobile paper overlay, shows a reviewable interpretation, and returns to the same World with the demo context intact. The preview remains write-free and states that context/participant selection belongs to the authenticated workflow.
