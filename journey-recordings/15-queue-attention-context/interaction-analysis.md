# Queue versus attention context

The World board shows three visible attention signals while the contextual responsibility queues are empty in the fixture. The recording verifies the revised empty-state explanation.

The important distinction is now explicit: `What needs attention` is the board’s derived attention surface; `Queues` is the participant-scoped responsibility projection. An empty queue no longer sounds like the whole World is clear. The copy points the user back to the board when a visible attention signal exists, without merging the two projections or changing backend semantics.

The MP4 is a live-tab viewport timeline.
