# Primary World rail matrix

Viewport: 1440×900, live browser device metrics. Dataset: `demo=goods_handoff`, starting from `slice=fixture:goods_handoff:execution&focus=evidence:1`.

Live results:

- Needs → `/world-engine/needs?...` → fixture, slice and focus preserved → close returns to the exact World URL.
- Commitments → `/world-engine/commitments?...` → same preservation and recovery.
- Evidence → `/world-engine/reality?...` → same preservation and recovery.
- Inbox → `/world-engine/inbox?...` → same preservation and recovery.

Each route presents a clearly labelled `WORLD · FIXTURE PREVIEW` overlay above the same corkboard. No second board or editor surface appears; the close affordance restores the same papers, threads and camera context.
