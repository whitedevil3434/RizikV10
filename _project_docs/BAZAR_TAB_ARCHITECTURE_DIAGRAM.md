# 🏗️ Bazar Tab Role-Based Filtering - Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                        │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │         FooddrobeScreen (Bazar Tab)                │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │  Role Indicator Header                   │     │    │
│  │  │  🙋‍♂️ খাবার ও পরিষেবা আবিষ্কার করুন [12] │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │  Masonry Grid                            │     │    │
│  │  │  ┌────┐ ┌────┐                          │     │    │
│  │  │  │Card│ │Card│  ← Filtered Content      │     │    │
│  │  │  └────┘ └────┘                          │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  └─────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────┘
                          ↑
                          │ Consumer<RoleProvider>
                          │ Consumer<FeedProvider>
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                      PROVIDER LAYER                          │
│                                                              │
│  ┌──────────────────┐         ┌──────────────────┐         │
│  │  RoleProvider    │         │  FeedProvider    │         │
│  │                  │         │                  │         │
│  │  _currentRole    │────────→│  _currentRole    │         │
│  │  (consumer/      │  sync   │  (consumer/      │         │
│  │   partner/       │         │   partner/       │         │
│  │   rider)         │         │   mover)         │         │
│  └──────────────────┘         └──────────────────┘         │
│                                        ↓                     │
│                          ┌─────────────────────────┐        │
│                          │  bazarFeedItems getter  │        │
│                          └─────────────────────────┘        │
│                                        ↓                     │
│                          ┌─────────────────────────┐        │
│                          │  Role-based filtering   │        │
│                          └─────────────────────────┘        │
│                                   ↓                          │
│              ┌────────────────────┼────────────────────┐    │
│              ↓                    ↓                    ↓    │
│   ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│   │ _getConsumer     │ │ _getPartner      │ │ _getMover        │
│   │ BazarItems()     │ │ BazarItems()     │ │ BazarItems()     │
│   └──────────────────┘ └──────────────────┘ └──────────────────┘
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                       DATA LAYER                             │
│                                                              │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│  │ _consumerFeed    │ │ _partnerFeed     │ │ _riderFeed       │
│  │ Items            │ │ Items            │ │ Items            │
│  │                  │ │                  │ │                  │
│  │ • FoodCardData   │ │ • EventCardData  │ │ • MissionCardData│
│  │ • ShopCardData   │ │   (Bid)          │ │ • EventCardData  │
│  │ • EventCardData  │ │ • EventCardData  │ │   (Bonus)        │
│  │ • RewardCardData │ │   (Unclaimed)    │ │ • RewardCardData │
│  │ • ReviewCardData │ │ • AISuggestCard  │ │                  │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘
└─────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
┌─────────────┐
│   User      │
│   Action    │
└──────┬──────┘
       │
       │ Taps role slider
       ↓
┌─────────────────────┐
│   RoleProvider      │
│   setRole(newRole)  │
└──────┬──────────────┘
       │
       │ notifyListeners()
       ↓
┌─────────────────────────────┐
│   FooddrobeScreen           │
│   Consumer<RoleProvider>    │
└──────┬──────────────────────┘
       │
       │ Detects role change
       ↓
┌─────────────────────────────┐
│   _syncRoleWithFeedProvider │
└──────┬──────────────────────┘
       │
       │ Maps role
       ↓
┌─────────────────────┐
│   FeedProvider      │
│   setRole(feedRole) │
└──────┬──────────────┘
       │
       │ notifyListeners()
       ↓
┌─────────────────────────────┐
│   FooddrobeScreen           │
│   Consumer<FeedProvider>    │
└──────┬──────────────────────┘
       │
       │ Reads bazarFeedItems
       ↓
┌─────────────────────────────┐
│   bazarFeedItems getter     │
│   switch(_currentRole)      │
└──────┬──────────────────────┘
       │
       ├─→ Consumer? → _getConsumerBazarItems()
       ├─→ Partner?  → _getPartnerBazarItems()
       └─→ Mover?    → _getMoverBazarItems()
       │
       │ Returns filtered List<FeedCard>
       ↓
┌─────────────────────┐
│   UI Rebuilds       │
│   with new content  │
└─────────────────────┘
```

---

## Filtering Logic Flow

### Consumer Filtering:
```
_consumerFeedItems
       ↓
   .where((item) {
       ↓
   if (item is FoodCardData) → ✅ KEEP
   if (item is ShopCardData) → ✅ KEEP
   if (item is EventCardData) → ✅ KEEP
   if (item is RewardCardData) → ✅ KEEP
   if (item is ReviewCardData) → ✅ KEEP
       ↓
   if (item is SquadManagementCardData) → ❌ REMOVE
   if (item is MealPlanStatusCardData) → ❌ REMOVE
   if (item is SocialLedgerCardData) → ❌ REMOVE
       ↓
   }).toList()
       ↓
   Return filtered list
```

### Partner Filtering:
```
_partnerFeedItems
       ↓
   .where((item) {
       ↓
   if (item is EventCardData) {
       if (eventType == 'Bid') → ✅ KEEP
       if (eventType == 'Unclaimed') → ✅ KEEP
       if (eventType == 'Missed Order') → ✅ KEEP
   }
   if (item is AISuggestCardData) → ✅ KEEP
   if (item is ReviewCardData) → ✅ KEEP
   if (item is RewardCardData) → ✅ KEEP
       ↓
   if (item is FoodCardData) → ❌ REMOVE
   if (item is ShopCardData) → ❌ REMOVE
       ↓
   }).toList()
       ↓
   Return filtered list
```

### Mover Filtering:
```
_riderFeedItems
       ↓
   .where((item) {
       ↓
   if (item is MissionCardData) → ✅ KEEP
   if (item is EventCardData) {
       if (eventType == 'Bonus') → ✅ KEEP
       if (eventType == 'Mission Chain') → ✅ KEEP
   }
   if (item is RewardCardData) → ✅ KEEP
       ↓
   if (item is FoodCardData) → ❌ REMOVE
   if (item is ShopCardData) → ❌ REMOVE
   if (item is ReviewCardData) → ❌ REMOVE
       ↓
   }).toList()
       ↓
   Return filtered list
```

---

## Role Mapping

```
┌─────────────────────┐         ┌─────────────────────┐
│   RoleProvider      │         │   FeedProvider      │
│   UserRole          │         │   UserRole          │
├─────────────────────┤         ├─────────────────────┤
│   .consumer         │────────→│   .consumer         │
│   .partner          │────────→│   .partner          │
│   .rider            │────────→│   .mover            │
└─────────────────────┘         └─────────────────────┘
```

**Note:** `rider` maps to `mover` in FeedProvider

---

## Component Interaction

```
┌──────────────────────────────────────────────────────┐
│                    main_screen.dart                   │
│                                                       │
│  ┌────────────────────────────────────────────┐     │
│  │  Bottom Navigation Bar                     │     │
│  │  [Home] [Bazar] [Create] [Orders] [Profile]│     │
│  └────────────────────────────────────────────┘     │
│                      ↓ Tap Bazar                     │
│  ┌────────────────────────────────────────────┐     │
│  │  FooddrobeScreen                           │     │
│  │  ┌──────────────────────────────────────┐ │     │
│  │  │  SearchFilterBar                     │ │     │
│  │  └──────────────────────────────────────┘ │     │
│  │  ┌──────────────────────────────────────┐ │     │
│  │  │  TabBarView                          │ │     │
│  │  │  • For You (Role-based)              │ │     │
│  │  │  • Stores                            │ │     │
│  │  │  • Reviews                           │ │     │
│  │  └──────────────────────────────────────┘ │     │
│  └────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────┘
```

---

## State Management

```
┌─────────────────────────────────────────────────────┐
│              Provider Hierarchy                      │
│                                                      │
│  MultiProvider                                       │
│  ├─ RoleProvider                                     │
│  │  └─ _currentRole: UserRole                       │
│  │                                                   │
│  ├─ FeedProvider                                     │
│  │  ├─ _currentRole: UserRole                       │
│  │  ├─ _consumerFeedItems: List<FeedCard>           │
│  │  ├─ _partnerFeedItems: List<FeedCard>            │
│  │  └─ _riderFeedItems: List<FeedCard>              │
│  │                                                   │
│  └─ Other Providers...                               │
└─────────────────────────────────────────────────────┘
```

---

## Performance Optimization

```
┌─────────────────────────────────────────────────────┐
│              Optimization Strategy                   │
│                                                      │
│  1. Lazy Filtering                                   │
│     • Filtering happens only when getter is called   │
│     • No pre-computation                             │
│                                                      │
│  2. Immutable Lists                                  │
│     • List.unmodifiable() prevents mutations         │
│     • Safe to pass around                            │
│                                                      │
│  3. Efficient Type Checking                          │
│     • Uses 'is' operator (O(1))                      │
│     • No string comparisons                          │
│                                                      │
│  4. Single Source of Truth                           │
│     • Role stored in one place                       │
│     • Synced automatically                           │
│                                                      │
│  5. Minimal Rebuilds                                 │
│     • Only affected widgets rebuild                  │
│     • Consumer scoping                               │
└─────────────────────────────────────────────────────┘
```

---

## Future Database Integration

```
┌─────────────────────────────────────────────────────┐
│           Future Supabase Integration                │
│                                                      │
│  FeedProvider                                        │
│       ↓                                              │
│  fetchBazarItems(UserRole role)                      │
│       ↓                                              │
│  Supabase Query:                                     │
│                                                      │
│  Consumer:                                           │
│    SELECT * FROM products                            │
│    WHERE is_sellable = TRUE                          │
│      AND type IN ('INSTANT_FOOD', 'SUBSCRIPTION')    │
│      AND area = user.area                            │
│                                                      │
│  Partner:                                            │
│    SELECT * FROM bids                                │
│    WHERE status = 'OPEN'                             │
│      AND area IN (partner.service_areas)             │
│                                                      │
│  Mover:                                              │
│    SELECT * FROM missions                            │
│    WHERE status = 'AVAILABLE'                        │
│      AND type IN ('POINT_TO_POINT', 'CHAIN_BUNDLE')  │
│      AND distance < 10                               │
└─────────────────────────────────────────────────────┘
```

---

## Summary

This architecture provides:
- ✅ Clear separation of concerns
- ✅ Efficient data filtering
- ✅ Automatic role synchronization
- ✅ Scalable for future features
- ✅ Easy to test and maintain
- ✅ Ready for database integration
