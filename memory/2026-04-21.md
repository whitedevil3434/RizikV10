# 2026-04-21

- Integrated `blader/humanizer`-inspired anti-AI cleanup stage into Ghost Writer pipeline:
  - New module: `backend/src/ghost/bladerHumanizer.ts`
  - Wired into `backend/src/ghost/transformEngine.ts` (opt-out via `options.bladerHumanizer: false`)
- Updated Writer UI to control the flag:
  - `rizik_saas/src/app/writer/page.tsx` adds “Anti-AI Humanizer” toggle and sends `bladerHumanizer`.
- Added regression coverage + short docs:
  - `backend/scripts/test_humanizer_policy.mjs` now tests the new cleanup stage.
  - `backend/docs/GHOST_HUMANIZER.md` describes behavior + option.
- Fixed a TypeScript compile blocker in `backend/src/ghost/humanErrorEngine.ts` (spread-arg typing) so `npm run test:quality` can run.

- Implemented missing Ghost V20 plan items:
  - Backend: added `personaIntensity` + `guardDropEnabled` to `HumanErrorConfig` and wired them through `transformEngine.ts`.
  - Backend: added new mutation layers `rant_abbrev` (wdym/ppl/sth), `hyphen_descriptor`, `self_correction`, and enforced persona signature inclusion for stability.
  - Frontend: added Persona Intensity slider + Guard-Drop toggle in writer UI; API now sends those options.
  - Tests: expanded `backend/scripts/test_humanizer_policy.mjs` with V20 persona diversity + guard-drop tests.
  - Docs: added `backend/docs/GHOST_V20_MASTER_ASIAN_ENGINE.md`.

- OpenClaw gateway bring-up (no version upgrade):
  - OpenClaw CLI was failing to start due to a corrupted `@clack/core` dist file (bad emoji regex escapes + invalid `node:util` import).
  - Created backup: `~/.openclaw/lib/node_modules/openclaw/node_modules/@clack/core/dist/index.mjs.bak-2026-04-21`.
  - Hotfixed `~/.openclaw/lib/node_modules/openclaw/node_modules/@clack/core/dist/index.mjs` so the CLI can run again.
  - Confirmed gateway service already running on `127.0.0.1:18789` and `openclaw gateway health` returns OK; Telegram channel OK, iMessage missing binary (`imsg`).
