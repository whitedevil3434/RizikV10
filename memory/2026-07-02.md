# Daily Log - 2026-07-02

## Actions Taken
- Downloaded the Hugging Face model `naver/splade-cocondenser-ensembledistil`.
  - Created an isolated Python virtual environment at `tmp/venv_hf` to bypass the system's externally managed environment restriction.
  - Successfully retrieved all model files using the official `huggingface_hub` `snapshot_download` function.
  - The model is stored locally at `/Users/sabbir/.cache/huggingface/hub/models--naver--splade-cocondenser-ensembledistil/snapshots/49cf4c7b0db5b870a401ddf5e2669993ef3699c7`.

- Diagnosed and fixed the Ollama `500 Internal Server Error: unable to load model: ... unknown model architecture: 'gemma4'` error:
  - **Root Cause**: Two Ollama server instances were running simultaneously:
    1. An older version (0.18.0) run by a launchd agent (`/Users/sabbir/.local/ollama/ollama serve`) binding to `localhost:11434` (IPv4).
    2. A modern version (0.31.1) run by the macOS Ollama app (`/Applications/Ollama.app`) binding to `*:11434` (IPv6).
  - The CLI `ollama` command defaulted to talking to the 0.18.0 instance on IPv4, which does not support the newer `gemma4` architecture.
  - **Fix**:
    - Unloaded and disabled the old launch agent plist by renaming it to `local.ollama.serve.plist.disabled`.
    - Restarted the modern `Ollama.app` (0.31.1) so it bound to both IPv4 and IPv6 on port 11434.
    - Updated the user CLI symlink at `/Users/sabbir/.local/bin/ollama` to point to the modern 0.31.1 CLI binary.
    - Verified that `ollama version` is aligned at `0.31.1` and `fredrezones55/Gemma-4-Uncensored-HauhauCS-Aggressive:e2b` loads and executes successfully without errors.
