# 2026-02-17 Daily Log

## LiveKit browser automation test

- Ran headless browser automation with Playwright from local machine.
- Verified LiveKit playground endpoints reachable:
  - https://agents-playground.livekit.io/
  - https://livekit.io/connection-test
- Generated token via deployed endpoint: `https://link-call-web.vercel.app/api/token`.
- Decoded token claims: roomJoin=true, room matched, participant matched.
- Browser-evaluated LiveKit connection test passed:
  - connection state: connected
  - localIdentity set correctly
  - remoteCount: 0 (no agent participant auto-joined)
- Evidence files:
  - /Users/sabbir/RizikV10/_project_docs/previews/livekit_agents_playground_check_2026-02-17.png
  - /Users/sabbir/RizikV10/_project_docs/previews/livekit_connection_test_page_2026-02-17.png
  - /Users/sabbir/RizikV10/_project_docs/previews/livekit_browser_automation_result_2026-02-17.png
  - /tmp/livekit_browser_test_result.json
- Added LiveKit agent auto-dispatch logic to Next token API (`link-call-web/src/app/api/token/route.ts`) using `RoomServiceClient.createDispatch`.
- Added compatibility alias route `/api/livekit/token` at `link-call-web/src/app/api/livekit/token/route.ts`.
- Added Flutter log support for token response `dispatched/dispatchError` in `lib/services/livekit_service.dart`.
- Switched Cloudflare worker token API to support both `/api/livekit/token` and `/api/token`.
- Added Cloudflare-side LiveKit agent auto-dispatch in `backend/src/index.ts` with response fields: `dispatched`, `dispatchError`.
- Added worker vars in `backend/wrangler.toml`: `LIVEKIT_URL`, `LIVEKIT_AGENT_NAME`.
- Local wrangler verification passed with real vars: both endpoints returned `hasToken=true` and `dispatched=true`.
- Removed temporary `backend/.dev.vars` after verification to avoid secret file persistence.
- Deployed Cloudflare worker `rizik-backend` (version 92a24749-1f4c-4131-9032-51840cad63e8) with token+dispatch support on `/api/livekit/token` and `/api/token`.
- Added legacy `SquadRoom` export to satisfy existing DO migration dependency during deploy.
- Set worker secrets `LIVEKIT_API_KEY` and `LIVEKIT_API_SECRET` via Cloudflare API.
- Live Cloudflare endpoint test passed: `dispatched=true`.
- Browser automation against Cloudflare token endpoint confirmed remote agent joined (`remoteNow=1`).

## Voice AI provider switch (Gemini -> SiliconFlow-ready)

- Updated `/backend/agent_python/main.py` to support env-based provider switching:
  - `VOICE_AI_PROVIDER=google` -> existing Gemini native realtime path.
  - `VOICE_AI_PROVIDER=siliconflow` -> OpenAI-compatible realtime client path for Qwen Omni.
- Added OpenAI-compatible plugin dependency:
  - `/backend/agent_python/requirements.txt`: `livekit-plugins-openai>=1.2.8`.
- Extended `/backend/agent_python/.env.example` with SiliconFlow settings:
  - `SILICONFLOW_API_KEY`, `SILICONFLOW_BASE_URL`, `SILICONFLOW_MODEL`, `SILICONFLOW_VOICE`.
- Smoke checks passed:
  - `python -m py_compile main.py` OK.
  - `_build_model()` works for both providers without runtime import errors.
- Note: Runtime auth to SiliconFlow endpoint could not be verified with currently available keys (HTTP 401/403), so live call depends on valid active SiliconFlow API key.

## SiliconFlow key region + WAF + voice pipeline

- Verified key `sk-avp...` works on `https://api.siliconflow.com/v1/models` (HTTP 200) via curl; `api.siliconflow.cn` and `api-st.siliconflow.cn` return 401 invalid.
- Earlier 403/1010 was due to Cloudflare/WAF blocking python-urllib default User-Agent; adding browser-like UA resolves.
- Verified Qwen3-Omni chat streaming works via SSE (`stream=true`) on `.com` endpoint.
- Updated `/backend/agent_python/main.py` SiliconFlow mode to use practical voice pipeline:
  - STT: `FunAudioLLM/SenseVoiceSmall`
  - LLM: `Qwen/Qwen3-Omni-30B-A3B-Instruct`
  - TTS: `FunAudioLLM/CosyVoice2-0.5B`
  (since `/v1/realtime` websocket style returned 404).
