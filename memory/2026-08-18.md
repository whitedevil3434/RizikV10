# 2026-08-18

- C-Link World-shell consolidation continued in `clink-platform`.
- The production boundary is now explicit: one persistent Pixi World owns material/camera/pins/yarn; React operational overlays own real text, forms and record actions; Protocol owns canonical facts.
- Legacy `CorkboardJourney`, `CorkboardQueue` and `CorkboardReality` do not mount a second material board inside `PersistentWorldProvider`; standalone visual-lab use remains intact.
- All direct Need/Commitment detail links now normalize through `worldFocusedRecordPath`: `/clink/world/record/<type>/<id>?focus=<typed-node>&detail=1`, preserving other URL state. This binds deep links, selected Pixi node, camera focus and React record overlay to the same typed canonical record.
- Validation: web TypeScript passed; 197 web tests passed; production build passed; `git diff --check` passed. Browser world-engine route verified a focused commitment and a direct record overlay while the same Pixi canvas remained mounted. No authenticated live-world validation or deployment performed.
- Follow-up audit found `CorkboardContextSlice` still rendered a second DOM material board inside the Business Profile World overlay. It now uses the persistent-World boundary, avoids its three duplicate list fetches there and emits only a compact semantic context summary. Web TypeScript, 198 tests and production build passed. Production browser session currently stops at the pre-existing "data service temporarily unavailable" workspace bootstrap screen after retry, so authenticated live route/camera verification remains unproven.
- Fixed queue/deep-record close semantics: `worldFocusReturnPath` retains slice/focus and removes only `detail`/one-shot capture. Both Live and demo Pixi route openers now encode the target node's typed focus state before opening its overlay. Mounted demo browser proof: direct focused Commitment record overlay contained the same canvas; close returned to `world-engine?slice=region:project&focus=commitment:interaction-model`, with canvas and selected focus still present and overlay gone. Web TypeScript + 199 tests + diff check pass; production build will need rerun after any concurrent changes.
- Production availability diagnosis (read-only): `https://rizikecosystem.com/api/clink/v1/health` returns 200, but `/health/ready` returns 503 with `{status:"not_ready", database:"unavailable"}` and Retry-After 60. This explains the authenticated browser's workspace bootstrap failure; it is a deployed database readiness issue, not a Pixi/route issue. No production mutation taken. Visual-lab copy was corrected to call the DOM corkboard a legacy material study rather than a production-record surface.
- Continued the World-continuity audit: Need Inbox, Commitment Inbox, Execution Queue and Need → Commitment handoff no longer send users through legacy `/clink/needs/*` or `/clink/commitments/*` URLs. They now use typed World links which preserve slice/focus/lens/temporal context and open the persistent World detail surface. Added `worldOverlayPath` for non-record workbenches such as New Need, plus regression coverage. Web tests now pass 202/202, TypeScript and production build pass, and `git diff --check` passes. Browser demo proof: direct focused record overlay closed back to `/clink/world-engine?slice=region:project&focus=commitment:interaction-model&lens=evidence`; same canvas and focus remained mounted.
- Visual entry audit found and fixed an actual World-startup defect: the `initialFocusId` selected by fixtures/live scenes was immediately overwritten by `fitWorld`, leaving an almost blank regions-only atlas. `PixiWorldSurface` now honors its safe initial scene anchor; live scenes nominate the current Actor. Focus-composition spacing was tightened and unrelated-card/yarn fade rebalanced so the first view reads as a populated corkboard slice. Local browser QA at 1280×720 showed 13 projected work cards, 2 active yarns, 0.50 zoom/work LOD and 71fps/13.6ms p95 after settling. TypeScript, 204 web tests, production build and diff check pass. No deploy attempted; live production database readiness is still independent/unavailable.
- Execution audit: the visual goods-handoff fixture previously skipped Need, Alignment, Obligation, Required Capability, Assignment and Execution. `createFixtureWorld` now projects the complete universal spine and typed links, so it can prove the protocol grammar rather than only show Commitment/Evidence/Outcome. The Living Record now recognizes an organization carrying obligated/responsible Actor roles, and does not treat a waiting outcome target as an observation. Fixture evidence now carries domain-specific observed facts (e.g. `498.4 kg recorded at receiving handoff`) rather than generic evidence copy. Validated: TypeScript, 206 web tests, production build and diff check pass.
- Material audit confirmed the production Pixi engine loads C-Link-owned generated cork/paper/pin+tape/yarn/wear WebP atlases (about 1.6 MB total) and maintains texture-based yarn shadows plus under/over yarn occlusion; it is not a CSS-drawn corkboard. Added a reusable textured paper contact-shadow layer to every Pixi card, using the alpha/torn-edge silhouette from the actual paper atlas and the shared upper-left lighting direction. Local viewport QA on `/clink/world-engine` was completed, then browser tab and Vite server were closed. TypeScript, 206 web tests, build and diff check pass.
- Added deterministic per-record pin and tape frame selection from `materialSeed`, preventing same-type cards from looking stamped from a single sprite while keeping their physical appearance stable across all slices. Concurrent work intentionally raised focused camera cap from 0.62 to 0.9 to cross readable work LOD; aligned its outdated test contract. Goods-handoff visual QA after this showed 17 canonical records, 13 projected cards, 4 semantic threads and 60fps with the complete execution grammar visible. TypeScript, 206 tests, build and diff check pass.
- Golden Screen material pass continued: actual `wear-atlas` chalk-circle now forms the selected-card halo beneath paper; display type shifted to Fraunces while metadata stays Inter; foreground yarn remains a short selected-focus cue only. The original generated pin source contained coloured studio glow in nearly transparent texels, creating visible edge haze in WebGL. Added `apps/web/scripts/clean-pin-atlas.py`, a deterministic Pillow asset-cleaner that neutralises weak-alpha RGB only in pin rows (tape stays untouched), and regenerated `public/clink-materials/v1/pin-tape-atlas.webp`; manifest now records 152,924 bytes / SHA `8e50fd...6dac5a`, total runtime materials 1,565,696 bytes. Browser Goods Handoff QA: 68fps, 17 records, 13 projected cards, 4 threads, coloured pin haze removed.
- Refined physical-pin hierarchy: a normal card keeps one actual material pin; graph ports remain semantic but relationship pins appear only on the selected card, max two, and only for yarn currently rendered. This removed the visible port-pin explosion without deleting any relationships/evidence. Latest web check, 206 unit tests, production build and diff check pass. Shared worktree remains intentionally dirty from parallel work; do not reset unrelated files. A local Vite session and Browser QA tab may still need to be stopped/closed at handoff.
- World-shell composition pass: normal public view now hides renderer FPS/route diagnostics; they remain intentionally available only with `?debugWorld=1`. Topbar uses the C-Link cork texture as material backing, while the focus-state rail, slice switcher and Living Detail panel now use real paper-atlas material layers instead of generic translucent dashboard chrome. Browser QA on isolated Vite: goods handoff stayed at 73fps/WebGL, and service-milestone plus AI-responsibility each switched datasets while preserving the same canvas/shell. TypeScript, 206 tests, production build and diff check pass. Closed QA browser and only stopped the Vite process started for this pass; a separate existing process on port 5174 was left untouched.
## C-Link world engine — focus composition validation

- Local visual QA continued with live Vite at `localhost:5173`. The Goods, Service Milestone, and AI Responsibility fixtures were walked through as full 12-stage coordination stories. A human-readability defect was found: fixture cards used bare ontology titles (Need, Capability, Alignment, Assignment, Outcome), so a person could not tell what was actually happening. `world-engine/model.ts` now supplies fixture-only human wording (e.g. "Receive 500 kg refrigerated goods", "Supplier assigned to handoff", "Human acceptance pending") and readable evidence labels without changing the universal engine/schema.
- World detail action copy was clarified from ambiguous `Act` / `Connections 9` to `Open commitment` / `View connections 9`. Outcome focus previously allowed the orientation minimap to cover a real Commitment paper; map now renders only at World/Slice orientation, while Focus/Detail retains breadcrumb + journey for local orientation. This was visually retested against the AI outcome flow at desktop and 390×844 mobile. Web no-emit TypeScript and 211 tests pass; normal build can be constrained by host disk capacity (only ~131 MiB free), not a source failure.

- Continued the unified Pixi world work. The core route remains one persistent Pixi surface with React detail overlays; the old corkboard components are not mounted as a second board in this surface.
- Improved the execution-focus semantic ordering so a commitment focus is composed as a human-readable execution story rather than a nearest-graph traversal. The universal resolver now follows indirect `Commitment → Alignment → Need` links and accepts both `actor` and `organization` projections for responsibility ownership.
- Commitment priority is now `Alignment → Need → Capability → Obligation → Responsibility → Assignment → Execution → Evidence → Outcome`; focus composition has a larger desktop card budget (9 visible cards total) and mobile budget 6.
- Added/kept a regression test proving the focus order for the goods-handoff fixture. Validation passed: `pnpm --filter @clink/web check`, `pnpm --filter @clink/web test` (17 files / 207 tests), `pnpm --filter @clink/web build`, and `git diff --check`.
- Visual QA in the renderer lab showed the commitment-focused board as a coherent pinned sequence: alignment, need, capability, commitment, responsibility, assignment, execution, evidence, outcome. The common connected-world demo itself does not currently project an explicit obligation card; this is a data/model completeness concern for that demo, not a second-board issue.
- Started and then stopped a private local Vite QA server on port 5181 (PID 87816) after testing; no deployment performed.
- Closed a demo-world completeness gap: `createConnectedExecutionWorld()` now materialises `Approval obligation` and `Review authority`, linked by `obligates`, `carries_obligation`, and `requires_capability`. This means the connected C-Link example uses the same 12-stage grammar as goods/service/AI fixtures.
- Increased desktop focus composition to 10 cards so commitment focus retains the final Outcome (mobile stays at 6). Visual QA confirmed commitment focus now shows alignment, need, capability, commitment, obligation, responsibility, assignment, execution, evidence, and outcome. Required capability remains in the journey/detail rail where it belongs when the focused canvas uses its finite reading budget.
- Added a regression test for connected-world grammar and focus priority. Check + test now pass at 208 web tests; production build and diff check pass. A private Vite instance on 5174 (PID 95323) was stopped after QA.
- Full visual fixture QA completed through the actual Pixi demo menu: goods handoff, service milestone, and AI responsibility each render a readable commitment-focused chain with their domain-specific evidence/outcome language. Goods: quantity/custody; service: artifact/client acceptance; AI: generated artifact/human acceptance.
- Fixed the examples-menu interaction: selecting a fixture now closes its `<details>` menu immediately, so the menu does not cover the newly composed World. Checked via actual click flow.
- Latest validation after this UX fix: web check, all 208 web tests, production build, and diff check pass. Private Vite QA server port 5181 (PID 97366) stopped after testing.
- Mobile pass: tested the World at 390×844. The focused World remains touch-oriented with semantic levels, slice chips, a material detail sheet, and the execution journey strip. Added active-stage auto-centering (`scrollIntoView` inside `CorkboardJourney`) plus horizontal scroll snap so a field operator does not have to manually search a long journey strip after focus changes.
- After mobile change: typecheck, all 208 web tests, production build, and diff check pass. Reset the browser viewport and stopped private QA Vite port 5181 (PID 98379).
- Audited the actual desktop Living Execution Record. It correctly exposes expected/observed, obligated actor, required capability, responsible party, uncertainties, next safe action, provenance, relationship path, and the focused world remains visible.
- Found and fixed a visual collision: the bottom journey strip ran underneath the expanded paper detail workbench. In detail mode it now reserves 384px for the detail panel and bounds the strip to the remaining safe area. Verified by screenshot.
- After the detail-layout correction: check, all 208 tests, production build, and diff check pass. Private QA Vite port 5181 (PID 1493) stopped.
- Scale QA: actual 10k connected World rendered 292 projected records at broad LOD. After warm-up it held ~70 FPS / 14.3ms p95; after an actual pan it held ~65 FPS / 14.1ms p95. At broad world scale no yarns render by design; the minimap carries the graph density.
- Extended `world-shell-architecture.test.ts` to lock the single-Pixi-World invariant for operational Activity Queue and Reality overlays too. Their legacy corkboard studies may exist outside WorldShell, but must return `null` inside it. Test count is now 209; check/build/diff pass.
- Cleared temporary 1024 viewport override and closed scale/mid QA tabs; private Vite port 5181 (PID 2399) stopped.

## World Engine — actionable participant queues

- Universal goods/service/AI fixtures now project all seven ActivityPub-inspired participant queues: Need Inbox, Commitment Inbox, Execution Inbox, Evidence Inbox, Execution Outbox, Evidence Outbox and Outcome Outbox. This makes the same canonical execution grammar operational from a participant view rather than only visually displayed.
- Corrected evidence semantics in the fixture projections: Evidence Inbox now surfaces a `waiting` evidence requirement (e.g. condition), while Evidence Outbox shows an already submitted/verified observation (e.g. quantity). A regression test locks this distinction across goods handoff, service milestone and AI responsibility fixtures.
- Queue browser QA on `/clink/world-engine`: Goods fixture shows 4 required actions and 7 visible queue records. Expanded Evidence Inbox exposes why/authority/evidence/privacy/next-safe-action and a `Capture evidence` action.
- Fixed the capture-intent continuity gap: the `capture=evidence` instruction is now preserved when a queue action opens the persistent World record route. Browser proof reached `/clink/world-engine/record/commitment/evidence%3A1?...&detail=1&capture=evidence`, titled `Evidence capture preview`, with the World retained below. The unauthenticated demo clearly identifies this as intent-preview only; actual canonical submission remains guarded/authenticated.
- Validation after this pass: Web TypeScript, 210/210 web tests, production build and `git diff --check` passed. A local QA tab/server may need closing at handoff.
- Follow-up corrected fixture routing fidelity: an Evidence/Execution/Outcome card remains its own spatial object, but its action now opens the parent `Commitment` workbench—the only aggregate that can accept evidence—matching live-world behavior. Browser proof: the Goods Evidence Inbox capture route is `/clink/world-engine/record/commitment/commitment%3Aprimary?...&focus=evidence%3A1&detail=1&capture=evidence`. Web TypeScript, 210 tests, production build and diff check pass.
- Extracted `worldRecordSearchForAction` as the single navigation rule shared by demo and live Pixi World. It preserves queue intent (`capture=evidence`) *and* existing lens, temporal/replay cursor, branch/geography and episode context while changing only spatial slice/focus/detail. A new regression catches the formerly hidden Evidence-lens loss. Validation: TypeScript, 211/211 web tests, production build and diff check pass.
- Outcome Outbox now opens the correct `workbench=outcome` intent (not generic action), and the Commitment detail reads/clears it to begin on the Outcome workbench. Queue labels now distinguish `Capture evidence`, `Open outcome`, and generic record opening. Closing removes transient capture/workbench state but preserves spatial focus.
- Fixed a major demo-world continuity bug: fixture selection is now URL-backed via `demo=<mode>`. Selecting a fixture clears incompatible former focus; record opening/reload preserves the selected fixture. Browser proof: Goods Handoff → Outcome Outbox → Open outcome → reload retained `demo=goods_handoff`, the 17-record Goods World, Outcome focus and Outcome workbench preview rather than falling back to Connected World. TypeScript, 211 tests, production build and diff check pass.
- Began cross-domain browser QA for goods/service/AI evidence/outcome routes. The broad three-fixture automation exceeded the in-app Browser’s 30s kernel cap after proving only initial selection behavior; do not claim three-browser-route proof yet. Fixture contract tests do cover all three queue memberships/actionability. Local QA Vite was stopped.

## World Engine — visual boundary and action-first navigation pass

- Local browser QA at `http://localhost:5173/clink/world-engine` found that the apparent giant diagonal/rectangular corkboard sheets were not data or yarn. They came from broad territory and ambient wear rendering, which made a continuous World surface look like several oversized panels at zoom-out.
- `PixiWorldEngine` now keeps the World and slice surface continuous: broad region contours are suppressed and `buildSliceTerritory` is intentionally a navigation-only no-op. The terrain no longer scatters generous wear-atlas source frames across the board; material wear stays contextual to an actual paper/selected record instead.
- Verified visually: continuous cork, no diagonal territory sheet; no browser console errors. Web TypeScript, 230 tests and production build pass.
- Topbar now makes the operational front door explicit: `+ New`, `Inbox`, and `Explore` remain visible; examples, camera/layout, saved views and lens controls moved into a single `More` menu. This preserves the feature set without making the World look like a developer toolbar.
