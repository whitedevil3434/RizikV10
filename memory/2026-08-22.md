# 2026-08-22

- C-Link visual QA confirmed ordinary card tap stays on the Living Mosaic as selection/attention shift; explicit OPEN is the only ordinary path to Work focus.
- Found and fixed relationship-focus scroll overlap: sticky parent Work context was covering the nested responsibility heading. Reduced local scroll cap from `.45` to `.36` in `clink-platform/apps/web/src/components/ActorRealitySurface.tsx`.
- Rechecked desktop and mobile screenshots; no console warnings/errors. Typecheck passed and Living Stage suite passed 66/66.
- Next visual cycle anchored the non-focused composer to the viewport bottom and proportionally stretched mobile mosaic rows; short 1024x600 desktop keeps its bounded fixed composition. Removed pointer focus-ring noise from board selection while preserving keyboard focus. Full suite passed 66/66, then targeted selection/breakpoint tests passed 4/4.
- Timeline visual audit checked Waiting → Advancing → Verified → Delivery Gap on mobile and desktop. Settled frames preserve header, semantic gap territory, bottom composer, and scrollY=0; added a regression invariant for that settled composition. Timeline targeted tests passed 2/2.
- Critical GAP audit found two issues and fixed them: focused desktop surfaces were incorrectly forced to 420px up to 760px height, and Mark unavailable retained Farm B's local scroll. Desktop focus now budgets viewport height through 850px, and GAP resets local scroll to preserve parent context. Mobile/desktop visual recheck passed; full Living Stage suite passed 66/66 and console stayed clean.
- Visual direction pivot: `/clink/actor?demo=relations` now mounts the territory-first Mosaic Engine against canonical relation data instead of the legacy dashboard. Active surface changed from purple prototype chrome to a warm neutral field with restrained semantic tones. Verified desktop/mobile initial state, blocked consequence reflow, and mobile local actor focus. The remaining work is to make the active surface less dashboard-like by deepening relationship geometry and resolving the compact parent rail on mobile.
- Next visual cycle rechecked all three states at desktop/mobile. Blocked reflow remained correct; mobile actor focus had unreadable horizontal parent context, so it now uses a readable two-column compact parent mosaic. Browser screenshot verification passed and web typecheck passed.
- Relationship audit found the territory was semantically sized but visually read as adjacent cards. Added restrained responsive causal seams between adjacent outer territories: initial Need → Capability → Actor/Responsibility, and blocked Need → Gap with downstream seams. Verified initial, blocked, recursive Actor focus, and recursive Commitment focus on desktop and mobile; the latter preserves a clear large state territory with compact parent context.
- New visual reference correction: focus must be spatial transformation, not a second mosaic/page. Refactored MosaicEnginePreview so the outer grid stays mounted during Actor/Commitment focus; the selected object claims a larger grid area and renders the inner Need/Capability/Responsibility/Commitment/Gap/Evidence mosaic inside its own boundary while neighbours compress. Verified Actor and Commitment expansion, mid-transition GSAP frame, mobile readability, and back contraction in the browser. Web typecheck passed.
- Blocked nested audit: desktop blocked Gap territory remains visible while Actor and Commitment expand in place; fresh settled frames showed no page replacement. Parent contraction followed by Reset Work restored the original waiting mosaic. Mobile blocked state was explicitly rechecked after a timing-ambiguous first toggle; Gap/Commitment territory correctly moved to the front and clearing the state collapsed focus as expected.
- Role-relative visual audit: Supplier, Restaurant, and Transporter settled surfaces preserve the same spatial field while labels, capability, responsibility, commitment, gap, and attention copy change. Fixed a mismatch where active header identity stayed SUPPLIER after role switch; nested focus back-context and Actor identity now derive from the active perspective. Desktop/mobile screenshots and web typecheck passed.
- New architecture reference emphasized coordinate ownership and persistent DOM identity. Converted collapsed outer nodes from buttons to persistent div role-buttons so the same Actor/Commitment element remains mounted as it expands; child mosaic is constrained by that selected grid territory, not a viewport surface. Browser screenshot shows Actor expanding in place with siblings sacrificing territory and inner mosaic visible inside it; typecheck passed.
- Focused role-switch audit found child content disappeared immediately when changing perspective. Added a closing-focus phase: selected territory contracts first, inner mosaic resolves cleanly, then the new perspective settles. Desktop/mobile transition frames and final Restaurant/Transporter surfaces were visually checked; web typecheck passed.
- Implemented the next semantic state pass in `MosaicEnginePreview`: WAITING/initial → ACCEPTED → BLOCKED → RESOLVED now uses state-derived attention signals and a shared row-territory calculation instead of state-specific grid maps. The legacy blocked `grid-template-areas` override was neutralized for ordinary state transitions; focus remains the only topology-changing interaction. Added state-aware action labels and neutral accepted/resolved semantic styling.
- Browser audit verified desktop and 390px mobile initial/accepted/blocked/resolved states, with blocked Gap + Commitment gaining territory while the parent Work remains mounted. Recursive Actor → Commitment focus was visually audited; mobile needed a final-width local Actor territory override to prevent the nested mosaic collapsing into the old multi-column grid. Typecheck passed after the changes. Minor dense-label polish remains in the deepest nested Commitment mosaic.
- Dense route audit found `/clink/actor?demo=dense` was still the old purple dashboard. Replaced that demo surface with `LivingMosaicWorks`: attention-scored Works now receive calculated grid spans, blocked Work gets dominant territory, low-attention active/done Works compress with reduced content density, and selection expands the selected Work in-place with a local consequence surface. Desktop and mobile screenshots were checked; selected blocked Work now exposes its local consequence without leaving the field. Typecheck passed. A pointer-focus outline was removed from dense tile clicks while retaining keyboard focus-visible behavior.
- Added `demo=dense100` as a 100-Work compression audit. Browser evidence showed two blocked Works dominate the first territory, waiting Works receive medium tiles, and low-attention active/done Works compress into compact readable tiles inside a finite scroll field. Cross-perspective browser audit also confirmed blocked state and 500 KG Gap persist when switching Supplier → Restaurant. Resolved state now morphs Evidence from `—` to `PROOF ✓` in outer and nested commitment territories; typecheck passed.
- Added a same-surface temporal lens to `MosaicEnginePreview`: PAST/NOW/FUTURE controls morph Commitment, Gap, and Evidence values without replacing the Work field. Desktop and 390px mobile temporal screenshots were checked; the rail wraps cleanly above the footer controls and Past/Future typography remains readable. A subsequent desktop nested-focus audit caught mobile-only Actor width/row overrides leaking into desktop; removed the inline mobile geometry and rechecked desktop Actor territory at 542×463 with inner Commitment/Evidence/Gap visible. Typecheck passed.
- Read the full role/relationship attachment. It clarified that role is Work/relationship context, not global identity; perspective controls are simulation only. Browser audit found `dense100` was still a 100-tile scroll list, so `LivingMosaicWorks` now compresses large sets into bounded semantic territories (Critical / Attention / Routine) and resolves up to 8 local Works in-place. Global page scroll was removed for large mode. Relation surface labels now say shared Work perspective instead of role view. Typecheck passed and browser verified Supplier → blocked → Restaurant perspective preserves the same Work and GAP consequence.
- Follow-up visual audit used 1280×720 and 390×844 browser viewports. Large mode initially clipped the composer on both budgets; adjusted field heights to keep the stage bounded and composer visible. Mobile local resolution briefly clipped its last row, so local sample depth was reduced to 6 Works. Rechecked desktop/mobile initial and local states: body height equals viewport, scrollY=0, no global scroll, composer visible, all local cards readable. Web typecheck remains green.
- Continued visual-first audit from fresh browser screenshots. Desktop large mode had an unintentional empty right half under the top territories; Routine was changed into a full-width low-density semantic band. Screenshot caught a count/action overlap, so the Routine micro-action was removed and the count/density hierarchy was rechecked. Fresh 1280×720 and 390×844 screenshots now show balanced bounded fields, visible composer, six readable local Works, bodyH=viewport, scrollY=0. Typecheck passed.
- Fresh primary relation audit found the normal mobile Work surface exceeded the viewport (`bodyH=883`) and clipped the action footer. Reduced only the non-focused mobile stage/grid from 450px to 400px; browser recheck now shows `bodyH=844`, `scrollY=0`, readable cards, temporal rail, and full action controls. Actor focus was then visually swiped through: its larger recursive territory remains intentionally scrollable, local Commitment/Evidence/Gap/links stay readable, and footer appears after the focus surface. Viewport reset and typecheck passed.
- State mutation browser audit rechecked desktop WAITING → ACCEPTED → BLOCKED → RESOLVED. Territory reflow and consequence colors were correct, but RESOLVED in the Gap tile wrapped as `RESOL / VED`; added compact resolved-gap typography. Rechecked the resolved frame at desktop and 390×844 mobile: both show a single readable `RESOLVED`, full controls, bodyH=viewport, scrollY=0. Typecheck passed.
- Motion-first audit captured the Actor focus at ~120ms and found the child mosaic painting densely over the still-reflowing parent/siblings. Added a delayed focus reveal: child content stays hidden during the territory claim and fades/scales in after the FLIP reflow. Browser recheck showed a clean empty claiming territory mid-frame and a readable settled Actor mosaic; typecheck passed.
- Follow-up motion audit visually captured Actor close/back and focused Supplier → Restaurant perspective change. Both contraction frames hide child content, preserve the parent field, and settle to a complete readable mosaic; no orphan child or page replacement appeared. Settled Restaurant perspective retained the same Work identity and context. No further code change was needed in this pass.
- Deep recursive mobile audit found Commitment focus was creating implicit outer grid rows: Need/Capability collapsed to ~30px and their text overlapped. Added explicit nested-focus row allocation plus zero implicit rows. Browser recheck at 390×844 showed outer rows 77/77/77/231/231/77px, readable parent and nested Commitment/Evidence/Gap, and after swipe the temporal rail/footer remained visible. Viewport reset and typecheck passed.
- Desktop side-effect audit after the nested-row fix checked Actor focus and nested Commitment focus at 1280×720. Both settled compositions remained readable; deeper Commitment content continues below the viewport as intentional local depth scroll, and a browser scroll exposes Evidence, GAP, canonical rail, temporal rail, attention, and footer. No desktop regression found.
- Dense semantic-territory motion audit checked desktop/mobile Attention → local resolution. Both emerged cleanly without list-scroll or viewport replacement. Mobile Work 14 focus then showed a blue pointer focus outline; added click-time blur on dense tile actions. Fresh mobile screenshot now shows selected local consequence with no focus noise, bodyH=viewport, and composer visible. Typecheck passed.
- Mobile temporal audit checked FUTURE, PAST, and blocked FUTURE combinations. Commitment/GAP morph values remained readable (`NEXT/RISK`, `RECORDED/—`, `RECOVERY/RECOVERY`), the temporal rail stayed inside the 400px primary stage budget, and footer/action controls remained visible at 390×844. Viewport reset after the audit.
- Short desktop audit at 1024×600 found the primary surface still overflowed (`bodyH=711`) because the 460px stage pushed temporal/actions below the viewport. Added a non-focused short-window budget: 350px stage/grid, tighter temporal/control spacing. Fresh browser recheck now gives `bodyH=600`, `scrollY=0`, readable initial and blocked states, temporal rail, and full actions. Viewport reset and typecheck passed.
- Compact mobile audit at 320×568 initially showed `bodyH=847` and a stage/footer below the viewport; the first 350ms screenshot also caught an in-progress FLIP frame, while the settled 900ms geometry was correct. Added a `max-width:340px` compact budget: 250px stage, smaller readable type/padding, compressed temporal/actions. Fresh settled initial and blocked screenshots now both fit exactly (`bodyH=568`, `scrollY=0`) with readable cards, temporal rail, and full action button. Viewport reset and typecheck passed.
- Compact 320×568 Actor-focus audit found outer Responsibility/Commitment text overflowing their intentionally compressed 52px sibling rows into the focus parent context. Added compact focused-sibling typography, padding, and overflow containment. Fresh browser screenshot shows the parent context line cleanly separated and sibling labels readable; viewport reset and typecheck passed.
- Live entry audit found `/clink` still rendered the legacy purple empty dashboard while the relation demo used Living Mosaic. Added `EmptyLivingWorkSurface` for the real no-Work entry: warm bounded field, first-need prompt, and Living Work composer. Browser verified `/clink` desktop, 390×844, and 320×568; tuned stage budgets until each measured `bodyH` exactly matched viewport with `scrollY=0` and full composer visible. Typecheck passed.
- Live empty composer interaction audit found the Ask C-Link button was a no-op. Added local open/input/intent-ready states without inventing backend Work data. Browser verified desktop and 320×568: tap opens the composer, typed intent shows `Intent ready to structure`, and compact mobile remains `bodyH=568`, `scrollY=0`, with the full composer visible. Viewport reset and typecheck passed.
### Legacy engine route audit
- Browser audit found `/clink/actor?demo=engine` still rendering the old purple `C-LINK / VISUAL ENGINE · PROTOTYPE` skin while `/clink` and `demo=relations` used the active warm Living Work surface.
- Updated `apps/web/src/components/ActorRealitySurface.tsx` so `engine`, `engine-relations`, and `relations` all render `MosaicEnginePreview` with `active` enabled. Deterministic demo data remains, but the legacy purple skin no longer appears in an active C-Link route.
- Browser rechecked desktop and 390px mobile: warm Living Work surface, no purple legacy visual, body height 844px and action controls remain inside the mobile viewport. Typecheck passes.
- Follow-up visual audit compared `engine-relations`, `relations`, `dense100`, and live `/clink` after settle. Desktop surfaces are bounded at 720px with the relation action layer visible after GSAP settles; dense100 compresses 100 Works into Critical/Attention/Routine territories; live entry remains warm and empty with composer in-frame. A screenshot taken mid-FLIP can temporarily hide the lower action layer, but settled geometry is correct.
- Computer-user mobile flow audit at 390x844 found focused Actor territory was correct structurally but compressed Responsibility/Commitment siblings tried to retain too much copy in ~52px rows. Added a mobile focused-sibling rule in `mosaic-engine-preview-final.css`: hide low-attention copy, reduce label/value typography, and clip overflow while retaining semantic label/value. Rechecked visible tap → focus/reflow → parent-context back → Accept commitment; focus exits, accepted state changes spatial emphasis and action label, body remains 844px. Typecheck passes.
- Nested Commitment computer-user audit at 390x844 found the focused parent was 349px wide but its inner deep mosaic was only 263px wide because the flex child stayed content-sized, leaving a large blank strip. Added `.mosaic-engine__focus-parent--commitment > .mosaic-engine__inner-grid { width: 100% }` to the final CSS. Rechecked screenshot: State/Evidence/Responsibility/Gap fill the parent territory; after a 600px swipe the lower temporal/action layer is readable and body height remains bounded at 1169px for the intentional focused surface. The final action click was not counted because the viewport reset before the target lookup; no false success claim.
- Repeated the nested Commitment action audit with viewport lifecycle kept intact. At 390x844, after swipe the `Accept commitment` button was visible at y775–810; CUA click produced accepted state and returned to the Work field. Subsequent CUA clicks visibly produced blocked (`GAP 500 KG`, `Resolve gap`) and resolved (`COMMITMENT RESOLVED`, `GAP RESOLVED`, `Reset Work`) states, each with body height 844px and no mobile overflow.
- Desktop regression audit at 1280x720 after the width fix: Commitment focus parent 421px wide and inner deep mosaic 393px wide, filling the territory; Evidence, Responsibility, Gap, temporal controls, and Accept action all remained visible with body height 720px and no page scroll. Screenshot shows the intentional low-attention outer Gap territory, not a clipped layout.
- Mobile perspective audit at 390x844: Actor focus → scroll → Restaurant perspective via CUA correctly collapses focus to the same Work field and changes perspective labels without replacing the Work. Found a 3px body overflow because the Restaurant subtitle wraps to two lines; reduced active mobile mosaic bottom padding by 4px in final CSS. Replayed the exact flow: body height is now 844px, scrollY 0, action layer visible. Typecheck passes.
- Compact 320x568 CUA audit: initial relation surface fits exactly with action controls visible; Actor focus keeps compressed siblings readable; deep lower surface exposes State/Evidence/Gap/Canonical Links/temporal/action layers without clipping; `Accept commitment` click exits focus to the 568px Work field and produces Accepted state with Block action visible. No code change required.
- Dense100 audit: at 390x844, Critical/Attention/Routine territories are readable and bounded; Critical click opens two local Work cards; selecting Work 37 expands it in-place with local consequence text and leaves the second card mounted; back returns to the territory field and composer remains visible. Desktop 1280x720 semantic field also fits exactly with no scroll. Typecheck passes.
- Temporal CUA audit at 390x844: PAST changes Commitment to RECORDED and Gap to historical `—`; FUTURE changes Commitment to NEXT and Gap to RISK; then Accept → Block while FUTURE is active produces Commitment/GAP RECOVERY and `Resolve gap`. All screenshots remain legible, body height stays 844px, and the temporal/action layer remains visible.
- Live entry CUA audit: `/clink` redirects to the warm Empty Living Work surface; at 390x844 composer opens, text entry is visible, and Send shows `Intent ready to structure` without creating a fake Work or leaving the viewport. Desktop 1280x720 empty field also keeps composer in-frame at y645–702 with body height 720. Typecheck passes.
- Tablet/short-desktop audit at 1024x600: initial relation stage is 350px high with temporal and action layers visible in a 600px viewport; Accept → Block keeps both Commitment and 500 KG Gap legible. Actor focus in blocked state creates the intended deeper local surface (body 701px), with no clipping; a 101px swipe exposes temporal and Resolve gap controls cleanly.
- Medium-density `demo=dense` audit: at 390x844, four Work cards plus Supplier/field-attention tiles and composer remain readable; selecting blocked Restaurant A expands locally to a 325px-wide consequence card while other Works remain mounted, and Close returns to the bounded field. Desktop 1280x720 mosaic fits with body height 720 and no scroll. No code change required.
- Dense100 Attention audit: at 390x844, the 72-Work Attention territory resolves to six readable local Work cards (bounded cap); Work 14 expands in-place with `NEXT ACTION` consequence while the other five remain mounted and composer stays in-frame. Back returns cleanly. Desktop 1280x720 semantic field remains bounded. Typecheck passes.
- Cross-perspective causal audit at 390x844: Supplier state advanced to Blocked, then CUA switched to Restaurant. Same Work remained `state-blocked`; Commitment stayed BLOCKED, Gap stayed 500 KG with consequence copy, and `Resolve gap` action persisted while labels/actor projection changed. Body remained 844px with scrollY 0.
- Dense100 Routine audit: at 390x844, Routine territory opens to six compact active Work cards rather than dead space; Work 12 expands in-place with `NEXT ACTION`, other Works remain mounted, composer stays visible, and Close returns to the territory field. Desktop 1280x720 semantic field remains bounded. Typecheck passes.
- Fallback route audit found three legacy purple leaks: `demo=unavailable`, `demo=syncing`, and `demo=timeline`. Normalized them: timeline now renders `LivingMosaicWorks` with timeline data; unavailable uses warm error-field overrides; syncing uses the warm Living Work palette while retaining emergence animation. Desktop screenshots show no purple shell; timeline is bounded at 720px. Typecheck passes. Mobile verification confirmed warm backgrounds, though the multi-tab viewport tool reported the active tab's 720px metrics for the first two captures; no false mobile sizing claim made.
- Syncing settled-frame audit at 390x844: after 4.5s, `C-LINK / READY`, Actors, Commitments, and `GAP CLEAR` are readable; `FIELD READY / Your Work field is ready.` is visible, background remains warm, body height exact 844px, scrollY 0. Initial and settled screenshots both show no purple legacy shell.
- 768x600 boundary audit found initial relation controls wrapped `Accept commitment` and body height was 602px. Added a narrow short-desktop rule: single-line action label, tighter padding. Fresh screenshot now shows body height exactly 600px, action one line, and a fresh Actor focus screenshot remains readable with intentional focused body height 701px and local controls below the fold. Typecheck passes.
- Exact 720x600 boundary audit after the same rule: initial body height is exactly 600px, Accept action stays one line, and fresh Actor focus remains readable with intentional body height 701px and controls below the fold. No additional breakpoint defect.
- Large desktop 1440x900 visual audit found Responsibility value `Farm B · 500 KG` wrapping into three lines because its font scaled beyond the fixed territory width. Added a responsive max cap/negative tracking for that node. Rechecked 1440x900 (two readable lines, body 900px) and 390x844 (still readable, body 844px). Typecheck and production build pass.
- Large desktop focus regression audit after the typography cap: Actor focus and nested Commitment focus both preserve readable inner mosaics at 1440x900; parent territory/inner grid widths are coherent, temporal/action layer is visible, and parent-context back returns to the Work field with body height 900px.
- Compact syncing audit found the transition preview's 500px stage caused body heights 792/769px at 320x568, clipping the lower emerging objects. Added a compact-only 320px stage/typography/footer budget. Fresh 320x568 CUA screenshots at initial and 4.5s settled frames now show `FIELD READY`, Commitments, Gap, and `Your Work field is ready` with body height exact 568px and scrollY 0. Typecheck and production build pass.
- Direct `/clink/world` browser audit: route resolves to `/clink/actor`, renders the warm Empty Living Work surface, has no canvas element, and exposes no Pixi/2.5D visual. Typecheck passes.
- Ran the web Vitest suite after route normalization: 42 files, 378 tests passed; 6 failures are all stale `src/routes/world-shell-architecture.test.ts` assertions expecting the retired Pixi/World shell and legacy route destinations. Current browser evidence confirms the intended invariant (World redirects, no canvas), so these failures are legacy-contract conflicts rather than a Living Mosaic runtime failure.
- Production build after the visual route normalization passes: TypeScript compile and Vite production bundle both succeed (`3554 modules transformed`).
- Syncing compact-boundary recheck: the initial 341×650 frame now uses the compact 320px stage and keeps the footer in-frame (`bodyH=650`, `stage=320px`, footer bottom≈463); the settled `FIELD READY` frame also remains exact-height with `scrollY=0`. The earlier narrow `max-width:340px` rule was broadened to `max-height:650px` so short phones wider than 340px receive the same bounded treatment. Typecheck and production build pass again.
- Follow-up visual/CUA audit: dense100 mobile and desktop territory fields both keep semantic compression, capped local cards, neighbour mounting, and the composer in-frame; selected Critical/Attention Work expands in-place with a visible local consequence. Relation demo at 390×844 showed Supplier → Restaurant perspective changes preserve the same Work projection and state. When a focused local territory was switched to Supplier, the invalid child focus collapsed to the nearest valid Work field, body returned from intentional focused depth (`1179px`) to exact viewport height (`844px`), and the Supplier perspective remained readable. No code change needed from this audit.
- Compact relation CUA audit at 320×568: initial Supplier perspective fits exactly with temporal rail and action visible. Commitment focus intentionally becomes a deeper local surface (`bodyH=1115`) with a blank territory-claim frame during reflow; a real swipe exposes State/Evidence/Responsibility/Gap and the action. CUA Accept then visibly collapses focus, changes Commitment to `ACCEPTED`, shows `Block commitment`, and returns to exact `bodyH=568`, `scrollY=0`. No visual regression or code change found.
- Live `/clink` CUA audit at 390×844 found the sent composer visually colliding with its own input: the long typed intent was truncated under `Intent ready to structure`. Updated `EmptyLivingWorkSurface` so sent state replaces the input with a single bounded confirmation label. Fresh CUA flow now shows a clean one-line composer (`Intent ready to structure`), no input overlap, `bodyH=844`, and no canvas/Pixi surface. Typecheck and production build pass (`3554 modules transformed`).
- Compact sent-composer follow-up at 320×568: after correcting an initial CUA miss by reading the actual button bounds, the send action was triggered successfully. Confirmation replaces the input cleanly, the arrow remains fully visible, composer bounds stay inside the viewport (`top≈503`, `bottom≈554`), `bodyH=568`, and `scrollY=0`.
- Timeline fallback visual audit at 390×844: route renders the warm Living Mosaic with no canvas/Pixi leak; two Works receive asymmetric territory from attention (`1000 KG` dominant, `300 KG` compact), identity/field-attention tiles remain mounted, and initial body height is exact 844px. CUA opening the dominant Work produces an in-place `NEXT ACTION` consequence with the neighbour and context tiles retained; focused depth grows intentionally (`bodyH=919`) rather than replacing the Work surface.
- Unavailable fallback CUA audit at 390×844: warm error field fills the viewport (`bodyH=844`, `scrollY=0`), error content, `Try again`, and composer remain visible, and no canvas/Pixi element is present. Retry was triggered with CUA and stayed on the deterministic unavailable demo as expected; no visual regression found.
- Legacy `/clink/world` mobile guard audit at 390×844: direct route resolves to `/clink/actor`, renders the warm Empty Living Work field, keeps `bodyH=844` and `scrollY=0`, and has no canvas or purple legacy visual. No code change needed.
- Short-desktop CUA audit at 768×600: initial and `ACCEPTED` relation states keep temporal controls and the full single-line action visible with exact `bodyH=600`; typography remains readable. CUA Actor focus expands the local Work territory while preserving parent siblings and yields intentional focused depth (`bodyH=701`) without clipping or viewport replacement. No code change needed.
- Deep comparison of the newly supplied role/responsibility specification against the repo: canonical backend primitives already cover Actor/Identity/Entity, contextual episode participation roles, responsibility/obligation/commitment, capability, delegation, evidence, outcomes, provenance, immutable events, snapshots, public share actions, and later claims. Active visual/runtime work covers actor-relative projection scaffolding, attention sorting, local territory expansion, GSAP/FLIP reflow, state mutation demos, semantic compression for 100 Works, and no Pixi/2.5D route. Critical gaps remain: live frontend still flattens needs with heuristic status/signals; real contextual responsibility/capability vocabulary is not yet projected into the active mosaic; no true attention frontier/cursor/pagination for 20k Works; no accountless multi-Work inbox; and no active UI proof for proposal conditions, delegation, recursive dependency Work creation, capacity pressure, or cross-actor synchronization. The supplied 2676-line attachment repeats the same specification previously audited, so findings are consistent.
- Implemented two alignment fixes: live `ActorRealitySurface` and `LivingMosaicWorks` no longer invent global `SUPPLIER` identity and now use the role-neutral `THIS WORK` anchor; `/world-projection` episode selection is actor-relative when `authenticatedActorId` exists, with a regression assertion in `world-projection-query.test.ts`. API query test now passes 9/9, web typecheck passes. CUA screenshot at 768×600 confirms `You · THIS WORK`, semantic cards, no canvas, exact `bodyH=600`; blocked Work expansion preserves siblings and shows local consequence in-place (`bodyH=665` intentional focus depth).
- Re-read the two latest attachments (895 lines/2,152 words and 2,676 lines/6,043 words; not duplicates). The newer short file reinforces the full model; the longer file adds the decisive scale correction: Mosaic is not a 20,000-Work board but a bounded viewport attention field. Required scale primitives are Attention Frontier, semantic resolution (Reality → Domain/Cluster → Actor → Work → Commitment → Evidence), temporary query surfaces, focus-path memory, since-last-visit change intelligence, event-derived state/time reconstruction, and local consequence propagation. Codebase has pieces of these (dashboard attention, advanced search, event/realtime/snapshot primitives, public share/claim) but the active `/clink/actor` Living Mosaic still lacks a real frontier/query/focus-path layer and maps projection needs with simplified heuristics. Existing `active_episodes limit 50` bounds the payload but is not yet an explicit cursor/frontier implementation.
- Fresh CUA visual audit of `dense100`: mobile 390×844 showed the semantic field, three bounded attention territories, no canvas, exact viewport height, and local Work expansion with `LOCAL CONSEQUENCE` while the neighbour remained mounted. Found and removed a redundant CSS pseudo-label that duplicated “bounded semantic compression” in the header. Rechecked mobile screenshot: copy appears once; cluster-to-local transition remains intact. Web typecheck and production build pass (`3554 modules`). Desktop DOM settled state reports exact `bodyH=720`, no filters/transforms, and three clusters; the desktop screenshot renderer appeared visually blurred despite clean computed styles, so no code change was inferred from that artifact.
- Follow-up CUA desktop audit exposed a real local-resolution geometry gap: after opening the Critical territory, two Work cards occupied only ~107px high at the top of the field, leaving dead space. Added `living-mosaic-works--local` state and responsive local tile geometry: desktop cards now claim ~282px vertical territory side-by-side; mobile cards claim ~233px while preserving the neighbour. Selected Work expands to ~313px with local consequence and the neighbour remains mounted. Desktop 1024×600 and mobile 390×844 screenshots both show exact viewport height and coherent territory use. Web typecheck passes.
- Relation CUA audit at 390×844: initial Work field fits exactly with all state/time/action controls visible; Accept changes Commitment to ACCEPTED and action to Block; Commitment focus creates the intentional deep surface (`bodyH=1165`), and a real swipe reaches State/Evidence/Responsibility/Gap/temporal/action controls. Found duplicate `GAP` labels in recursive focus: one parent Work gap and one inner Commitment dependency gap. Renamed inner labels to `DEPENDENCY GAP` in `MosaicEnginePreview.tsx`; rechecked screenshot shows the hierarchy clearly and no canvas. Web typecheck passes.
- Master System Builder goal audit: the live empty composer was confirmed by CUA at 320×568/390×844 to show a false local confirmation without a canonical Work. Integrated the truthful handoff: `EmptyLivingWorkSurface` now preserves intent and routes to `/clink/onboarding` when no workspace exists, or stores the workspace draft and routes to the canonical `/clink/needs/new` review pipeline when a workspace exists. Added the guarded `/clink/needs/new` route using `NewNeed presentation="world"`. CUA initially found the onboarding textarea blank due to a draft-key mismatch; fixed `Onboarding` to read `clink:onboarding:intent` as a fallback. Re-run at 390×844 shows the exact typed intent `Need 500kg rice delivered by Friday` in the onboarding textarea, with `Device draft · not shared`, no canvas, and no fake Work. Web typecheck passes. Backend creation was not submitted during QA to avoid an unrequested external workspace mutation.
- Re-verified the goal-objective file checksum and re-audited onboarding at 390×844 with CUA: preserved intent is visible, Continue button is enabled, device-draft status is explicit, body height is exact 844, and no canvas is rendered. Production build after the live handoff/onboarding fixes passes (`3563 modules transformed`). The canonical workspace/Need mutation remains intentionally unsubmitted in QA; the next evidence gate is a controlled test-environment creation followed by projection/event verification.
- Recovery-boundary CUA audit at 390×844: direct `/clink/needs/new` under unavailable workspace state shows a truthful reconnect surface with three readable actions; `Return to Living Mosaic` returns to `/clink/actor` with exact `bodyH=844` and no canvas; `Try again` reattempts without creating a fake Work and remains on the explicit unavailable state when the service is still down. No code change required.
- Final visual regression sweep after the handoff route: live `/clink` at 320×568 and relation demo at 390×844 remain exact-height, warm, and canvas-free; onboarding preserves the typed intent. Diff review found an empty legacy device-draft key could block the live intent fallback; changed onboarding draft selection from nullish to truthy fallback. CUA recheck at 390×844 confirms `Need 500kg rice delivered by Friday` remains visible with `Device draft · not shared`; web typecheck passes.
- Repeated goal-file verification and CUA sweep: live `/clink` 390×844 and dense100 attention surface 390×844 both remain exact-height, canvas-free, and visually stable; relation controls remain available. Onboarding draft fallback is now confirmed against the current runtime. `git diff --check` is clean and targeted web typecheck remains green.
- Master Builder continuation audit: re-read the locked goal (777 lines; SHA-256 `cfe3622f6bc76db825858039223294231a047eee7b3ba348fff4beffa01d1f00`) and inspected the active `dense100` surface with CUA at 390x844. The screenshot confirms a bounded three-territory attention field, no canvas/Pixi, body height exactly 844, and in-place local resolution. Code review confirms the major unresolved scale gap remains real: `/protocol/world-projection` silently limits `active_episodes` to 50 and the live actor projection has no explicit attention-frontier cursor/query contract; `LivingMosaicWorks` semantic compression is currently a frontend bounded demo, not a server-backed frontier. API tests passed 31 files/133 tests and web typecheck completed without error. No speculative frontier code was added in this pass; next implementation should define and test the frontier contract before wiring visual pagination.
- Implemented the next Master Builder correction: `/protocol/world-projection` now exposes an explicit actor-relative keyset Attention Frontier (`frontierLimit`, `frontierCursor`, `frontier-v1` continuation, `hasMore`, next cursor) instead of silently relying on a fixed 50-episode window. Canonical projection preserves `attentionFrontier` metadata; Living Mosaic/live actor copy marks a bounded frontier truthfully when more Work exists. Added API regression coverage. API suite passed 31 files/135 tests, web typecheck passed, `git diff --check` clean. CUA recheck at 390x844 on `dense100` remained exact-height, canvas-free, with 3 semantic territories. Full frontier pagination/merge and query-surface interaction remain open.
- Continued frontier implementation: added canonical `mergeCanonicalProjectionEnvelopes` with object/relationship/event de-duplication and later-page precedence; `ActorRealitySurface` now requests `frontier-v1` continuation pages and merges them without replacing the Work field. Large live projections route into `LivingMosaicWorks`, exposing `NEXT FRONTIER` only when `hasMore` exists. Added 155-test web merge regression. API check passed, targeted API projection tests passed 32/32, web check passed, and CUA dense100 mobile remained exact-height/canvas-free with 3 territories. Full pagination UX and focus-path persistence remain open.
- Focus-path audit and fix: `LivingMosaicWorks` now stores cluster + Work as one semantic focus path and reconciles it against changing cluster membership. If a focused Work moves from Critical to Attention, the local field follows it; if the Work disappears, the nearest valid parent remains or the path clears safely. Added `reconcileMosaicFocusPath` regression tests (3) and verified with CUA at 390x844: local Work focus, consequence territory, back contraction, exact body height, and no canvas/Pixi. Web targeted tests: 158/158 passed; web typecheck and diff check passed.
- Cross-actor synchronization audit found a real frontier bug: after a user had loaded continuation pages, a realtime event only refetched the current cursor page, leaving earlier merged pages stale. Fixed `ActorRealitySurface` to clear the cursor/page accumulator and invalidate the full actor-relative projection query on every workspace event, including replay-gap/stale recovery. Added a contract regression. Web targeted tests passed 159/159, API realtime tests 4/4, web typecheck and diff check passed. CUA dense100 mobile remained exact-height (844), canvas-free, and visually unchanged. A true two-authenticated-session mutation was not run because the local workspace service is unavailable; the canonical invalidation path is verified, but end-to-end multi-actor propagation remains an open evidence gate.
- Accountless multi-Work audit found saved guest links were not actually aggregated: `guest-claims` retained up to 12 tokens, but onboarding attached only the URL's single `claim` token. Added deterministic de-duplicated claim queue combining explicit and device-saved tokens; onboarding now attaches each permissioned claim, reports pending count, and retries only failed links. CUA onboarding at 390x844 remains exact-height, canvas-free, preserves the device draft and enabled Continue action. Guest claim tests and source contract tests passed 3/3; web typecheck and diff check passed. A real accountless multi-link backend claim was not submitted during QA, so server-side multi-claim evidence remains open.
- Explicit 20,000 Work stress audit completed. Added `demo=dense20k` with 20k deterministic Works and optimized Critical/Attention/Routine partitioning from repeated array membership scans to Sets. CUA mobile 390x844 and desktop 1280x720 both settled in ~2.2s with exact viewport height, scrollY 0, no canvas, 3 semantic territory nodes, and zero Work card DOM nodes initially; Attention local resolution mounted only 6 cards and remained bounded. Added scale contract tests; web typecheck and targeted tests passed 6/6. This proves frontend semantic compression behavior, not database-scale query/render performance; server 20k frontier benchmark remains open.
- Server-side scale evidence pass: local API/DB ports 8787/8788 were unavailable, so no fake database latency claim was made. Added deterministic `buildWorldProjection` benchmark for 20,000 canonical episode needs; it shaped 20k need objects in 15.9ms locally, with a 5s regression ceiling. Added `parseLimit(20000) -> 100` frontier boundary test. API check passed and targeted API tests passed 14/14. CUA recheck of dense20k mobile remains exact 844px, 3 territories, 0 Work-card DOM nodes, no canvas. Real Postgres/worker query latency remains an explicit open gate.
- Master Builder contextual projection pass: `ActorWork` now carries canonical contextual responsibility/capability/commitment fields derived from related objects plus actor-linked relationship types. Added projection regression coverage; web check and targeted suite passed 158/158. Wired the active Mosaic Engine demo to consume these fields in outer, Actor-focus, and nested Commitment tiles instead of hardcoded `500 KG`/`FARM B` values. Neutralized misleading global-role language to `Actor-relative projection` / `ACTOR PROJECTION`; demo perspective buttons remain explicitly simulation controls. CUA at 390x844 verified initial `bodyH=844`, `scrollY=0`, no canvas, canonical contextual copy (`Chicken supply`, `Supply 500 KG chicken`, `Farm B commitment`), and in-place Actor focus with `Work · Restaurant A` → `ACTOR · Farm B`, `bodyH=1165` only for intentional local depth. Real authenticated backend contextual payload remains an evidence gate.
- Backend truth-pipeline audit found a real SQL defect in `/protocol/world-projection`: the capabilities subquery selected `ac.capability_type` without joining `clink.actor_capacities`. Added the workspace-scoped left join and a route contract regression. API typecheck plus projection/query/20k scale tests passed 34/34. A bounded Wrangler startup attempt could not launch the local API because no Postgres connection string is configured; therefore authenticated DB/browser propagation remains explicitly unverified, not claimed as pass.
- Multi-commitment actor-relative audit found the frontend contextual commitment selector ignored the canonical `participates_in` relationship emitted by the API, so the first commitment could be shown as “mine” in a multi-party Work. Added `participates_in` matching and expanded the synthetic projection test with a second commitment; web typecheck and targeted tests passed 3/3. Fresh browser recheck remains warm, canvas-free, actor-relative, and shows contextual responsibility. Database integration remains skipped (5 tests) because `CLINK_DATABASE_URL`/runtime URL are unset.
- Identity boundary audit verified `/workspaces` returns the canonical runtime `clink.actors.id` as `actorId`, while `/protocol/world-projection` filters and emits the same runtime actor IDs. No party/runtime identity mismatch remains in the actor-relative selector path. Database integration suite was explicitly run and reported 5 skipped tests because both required connection URLs are unset; no live DB claim was made.
- Truth-boundary correction: contextual projection previously fell back to the first related responsibility/capability/commitment even when an authenticated actor had no explicit link, risking cross-actor data appearing as `YOUR`. Fallback is now allowed only for actor-less deterministic previews; live actor projections require explicit `responsible_for`/`provides`/`participates_in` links. Added a negative cross-actor regression alongside the positive multi-commitment test. Web typecheck, targeted projection/contract/engine suite passed 159/159; browser relation recheck is canvas-free, actor-relative, exact `scrollY=0`.
- Broad Master Builder regression: six obsolete Pixi/WorldShell architecture expectations were replaced with nine current Living Mosaic contracts covering actor-relative projection, explicit actor links, GSAP FLIP in-place focus, attention compression/frontier, truthful composer routing, and canvas-free active surface. Full workspace tests now pass: API 32 files/138 tests, web 45 files/350 tests, domain 7 files/40 tests, database 45 passed with 5 integration tests skipped because no Postgres URL is configured. Full production build and `git diff --check` pass. Live CUA then exposed a real remaining role-switch affordance in the active Mosaic demo; removed the Restaurant/Supplier/Transporter buttons, added a regression guard, rebuilt, and re-ran the full suite. Browser confirms zero role-switch buttons, zero canvas, exact 720px initial surface, and `Accept commitment` mutates `WAITING → ACCEPTED` in place. Authenticated DB propagation, proposal/counterproposal, delegation, recursive Work chaining, and true multi-browser synchronization remain open evidence gates.
- Added the mandatory typed operational QA matrix at `apps/api/src/application/operational-scenarios.ts`: 100 scenarios across procurement, logistics, service, student projects, and AI operations, each with initial state, actor context, action sequence, expected result, observed result, failure, severity, fix status, and regression key. The catalog is explicitly marked `NOT_RUN`/`PENDING`; it is not runtime proof. Added three invariant tests. Full workspace suite now passes API 33 files/141 tests, web 45 files/350 tests, domain 40 tests, database 45 passed with 5 integration skips; production build (3563 modules) and diff check pass. Fresh dense100 browser audit at 1280×720 confirms three semantic territories, exact viewport height, zero canvas, zero role-switch buttons. Next gate is controlled execution of the highest-risk scenarios against a real database/runtime fixture.
- Runtime prerequisite audit for the next gate: no `psql`, Docker, or Podman binary is available; no `CLINK_DATABASE_URL`, `DATABASE_URL`, or `CLINK_RUNTIME_DATABASE_URL` is configured. Repository integration tests require both connection URLs plus `CLINK_DATABASE_INTEGRATION_TESTS=1`, so they correctly remain skipped rather than silently touching an unknown database. Full tests/build still pass after the scenario catalog, and fresh CUA relation audit at 1280×720 confirms exact viewport height, actor-relative copy, zero canvas, zero global role buttons, and an available commitment action. Real DB/runtime scenario execution remains pending, not blocked or falsely passed.
- Truth-boundary correction in `MosaicEnginePreview`: replaced direct React `engineState` mutation with an explicit append-only `MosaicDemoEvent` stream and `reduceMosaicDemoState`/`nextMosaicDemoEvent` reducer. Added tests for ordered lifecycle, invalid out-of-order events, and explicit reset. Full workspace suite now passes API 33 files/141 tests and web 46 files/353 tests; web production build (3563 modules) passes. Browser replay at 1280×720 visibly produced `WAITING → ACCEPTED → BLOCKED → RESOLVED → WAITING` through the rendered action, with body height fixed at 720, scrollY 0, and zero canvas. This improves deterministic preview truth but is not a substitute for authenticated backend event propagation.
- Canonical action-boundary audit: actor-relative `contextual.responsibility`, `contextual.capability`, and `contextual.commitment` projections now retain each canonical object `id` alongside label/status/summary. Added positive ID assertions while preserving the negative cross-actor no-leak test; this prevents future action/proof UI from addressing records by display text. Full workspace suite passes API 33 files/141 tests and web 46 files/353 tests; web production build (3563 modules) and diff check pass. Browser relation surface remains actor-relative, responsibility-visible, canvas-free, exact 720px, with the commitment action present. Authenticated mutation/event propagation remains unverified pending DB/runtime fixture.
- Wired canonical identity into the active `LivingMosaicWorks` surface: every mounted tile now carries `data-canonical-work-id`, and a selected Work exposes contextual responsibility/commitment objects through `data-canonical-object-id` while preserving the local consequence composition. Added a source contract test. Full workspace suite now passes API 33 files/141 tests and web 46 files/354 tests; web build (3563 modules) and diff check pass. Browser dense100 audit confirms semantic compression starts with 0 Work tiles and 3 clusters, then mounts 2 addressable local Work tiles after territory resolution, with bodyH 720, scrollY 0, zero canvas, and zero global role buttons. Backend action mutation remains a separate pending gate.
- Replaced stale living-stage E2E assumptions about old card buttons and global role switching with 8 current Living Work contract groups; active E2E passes 16/16 across desktop and mobile. The run caught a real focused-mobile overflow (document height 1099px/928px), so focused local territory now scrolls internally under a viewport budget in mosaic-engine-preview-final.css; bounded focus, nested focus, lifecycle, temporal lens, and dense100 compression all pass. This is frontend/demo evidence only, not backend synchronization proof.
- Added two current active-surface E2E gates and verified desktop/mobile: `demo=dense20k` renders 20,000 Works as three semantic territories with zero Work-card DOM nodes initially, mounts only the local blocked frontier after territory resolution, and stays within 390x844; accountless `/clink` opens the in-place intent composer, then truthfully routes to `/clink/onboarding` with the draft preserved instead of claiming a canonical Work exists. The full active Living Work file now passes 20/20 across desktop and mobile. These are frontend/accountless-entry proofs, not database-backed commitment or cross-actor proofs.
- Backend authority audit found `POST /protocol/authorities` accepted `delegatedFromId` but never materialized `clink.delegation_edges`, so `enforceActorAuthority()` could not recognize the delegate. Added parent-grant existence/scope/grantor validation, delegate self-check, delegation-edge insertion, and delegation object reference in the append-only `authority.granted` event. API check passed, full API suite passed 141/141 before the contract assertion was added, and the authority governance contract now passes 4/4. Real DB insertion/authority execution remains unverified because no runtime database is configured.
- Temporal replay audit found the replay layer ignored canonical append positions even though projection events expose `eventPosition`; same-timestamp events could therefore be ordered by UUID instead of causal ledger order. Added `eventPosition`-aware ordering to `event-replay.ts` and an adversarial same-timestamp regression. API suite now passes 33 files / 143 tests, API typecheck and diff check pass. Live historical reconstruction remains unverified without a database/runtime fixture.
- Active-route audit found `/clink/needs/new` was still mounted with `presentation="world"`, so the real composer flow could return to legacy World after creation/cancel even though `/clink` itself redirected to Living Mosaic. Changed the active App route to `presentation="page"`; `NewNeed` now returns active presentation to `/clink/actor` while preserving the legacy WorldShell branch. Added architecture contract coverage. Web Vitest passes 46 files / 355 tests and active/public desktop+mobile E2E passes 28/28; web typecheck and diff check pass.
- Evidence provenance audit found `evidence.attached` events referenced the evidence row and optional obligation/execution scope but not the direct subject (commitment/outcome/etc.), weakening historical proof-chain reconstruction. Added canonical subject object refs to upload and written-record paths, and evidence refs to approval events; strengthened the evidence scope contract. API suite remains 33 files / 143 tests passed, typecheck and diff check pass. Live evidence/outcome runtime remains unverified without Postgres.
- Outcome provenance audit found `outcome.accepted/reported/disputed` events were aggregated under a commitment but omitted the commitment itself from `objectRefs`. Added `{ type: "commitment", role: "subject" }` and a regression contract. API suite now passes 33 files / 144 tests; typecheck and diff check pass. Live proof-chain reconstruction remains a database/runtime gate.
- Execution-change audit found proposal/decision/apply events and `execution.contract_created` also lacked direct parent-commitment refs despite using the commitment as aggregate. Added commitment subject refs across the lifecycle and a contract test. API suite now passes 33 files / 145 tests; typecheck and diff check pass. Live proposal/application runtime remains unverified without Postgres.
- Universal Work Simulation milestone: added `apps/api/src/application/universal-work-simulation.ts` as a deterministic, domain-neutral event reducer and actor-relative projector. The fixture models Restaurant → Supplier → Farm → Transporter, Supplier's internal 80 KG need, Farm feed dependency, and Restaurant Kitchen/Procurement consequences without global role switching. Tests prove one shared canonical Work, contextual responsibility, recursive dependency propagation, blocked Farm → at-risk Supplier/Restaurant consequences, and stable actor identity. A first test exposed missing `contains` propagation; adding it made the causal chain pass. API suite now passes 34 files / 148 tests and typecheck/diff check pass. This is simulation evidence only; live multi-actor DB propagation and cross-session realtime remain unverified because no Postgres runtime is configured.
- Public-share provenance follow-up: added direct commitment subject refs to public-share evidence, commitment-started, execution-observed, outcome, and commitment-accepted events. API suite remains 34 files / 148 tests and typecheck/diff check pass.
- Universal grammar recheck: added an unrelated Student Project fixture to the same reducer/projector. Research block now propagates to Presentation and the Student A consequence without any new role/component branch; API suite passes 34 files / 149 tests. Also routed all live canonical web projections through the domain-neutral `LivingMosaicWorks` renderer with `THIS WORK`; the old Restaurant/Farm-shaped `ActorRealitySurface` is now demo-only. Web suite passes 46 files / 356 tests and web typecheck/diff check pass. This removes a major live-product role/domain leak, while DB-backed propagation remains the next evidence gate.
- Causal projection correction: live `actorWorksFromProjection` now distinguishes a canonical Need that is itself blocked from an upstream Need whose related dependency is blocked. The latter materializes as `AT_RISK`, preserving canonical truth while increasing attention/territory. `LivingMosaicWorks` renders the generic downstream-risk state and no longer hardcodes a KG gap. Added a regression test. Web suite passes 46 files / 357 tests and web typecheck/diff check pass.
- Database contract recheck after the projection change: database package schema/RLS contract suite passes 45 tests, TypeScript check passes, and the 5 PostgreSQL integration tests remain correctly skipped because no connection URLs/runtime are configured. No live DB propagation claim is made.
- Moved derived risk into the backend read-model contract: `buildWorldProjection` now annotates a non-blocked Need with `derivedState: "at_risk"` when a connected canonical dependency is blocked, without mutating `lifecycleState`. Web canonical projection validation accepts only this explicit derived state, and the actor adapter consumes it while retaining fixture fallback coverage. API suite passes 34 files / 150 tests; web suite passes 46 files / 357 tests; both typechecks and diff check pass. Live database propagation remains unverified.
- Added `demo=universal&actor={restaurant|supplier|farm|transporter}` to render the same Universal Work scenario through the active `LivingMosaicWorks` surface, with no role-switch controls. Browser evidence: Restaurant shows 3 `AT RISK` Works (shared Need, kitchen consequence, replacement supply); Farm shows 2 `BLOCKED` Works; desktop 1280x720 has exact body height, scrollY 0, and zero role buttons. This is visual scenario evidence, not live DB state.
- Added `stage=accepted|blocked` to the Universal Work visual fixture. Browser transition evidence at Restaurant perspective: accepted stage renders 2 Works (`WAITING` Need + `ACTIVE` kitchen consequence), exact body height 720 and scrollY 0; blocked stage renders 3 Works with shared Need/kitchen/replacement all `AT RISK`, body height 768 and scrollY 0. The state change visibly materializes a new consequence Work and increases attention without a screen replacement.
- Replaced URL-only stage comparison with an in-place `UniversalWorkSimulationPreview`: one action advances accepted → blocked and uses the same `LivingMosaicWorks` mount. Added a geometry signature to the FLIP effect so status/attention changes with equal-length Work sets also animate. Browser evidence: before action 1 main/2 tiles/accepted; after action 1 main/3 tiles/blocked/3 at-risk, scrollY 0. Web suite passes 46 files / 358 tests and typecheck/diff check pass.
- Cross-actor browser simulation: Restaurant and Supplier tabs share the same simulated Universal Work state through `BroadcastChannel`; advancing Restaurant changed Supplier from accepted/2 tiles to blocked/3 tiles/2 at-risk with scrollY 0. This is an executable multi-view contract, explicitly separate from backend realtime proof.
- Replaced demo stage messages with an explicit append-only event stream (`responsibility.accepted`, `commitment.created`, `work.blocked`) and reducer. Browser recheck confirms the Supplier tab receives the event-derived blocked projection (accepted/2 tiles → blocked/3 tiles/2 at-risk); web suite now passes 47 files / 360 tests and typecheck/diff check pass. Backend realtime remains unverified.
- Added a real Playwright regression for the cross-actor Universal Work event path in `apps/web/tests/living-stage.spec.ts`. It opens Restaurant and Supplier projections in the same browser context, asserts Supplier starts at `SIMULATION · ACCEPTED` with 2 tiles, advances Restaurant, then asserts Supplier receives `SIMULATION · BLOCKED`, mounts 3 tiles, shows 2 downstream-risk consequences, and remains at `scrollY=0`. Full Living Stage E2E passes 22/22 across desktop and iPhone profiles; web Vitest remains 47 files / 360 tests, and `git diff --check` passes. This is browser/simulated BroadcastChannel evidence, not authenticated database realtime proof.
- Truth-layer hardening: Universal Work simulation events now honor canonical append `position`, ignore duplicate delivery by event id, and retain unrelated ledger events without mutating Work state. Added `protocol-universal-work.ts` to map canonical protocol vocabulary (`need.created`, `responsibility.accepted`, `commitment.accepted`, capability updates, evidence) into the domain-neutral reducer without role-specific branches. API suite passes 35 files / 153 tests; API typecheck and diff check pass. This strengthens event → state → projection proof, but the real Postgres/runtime path remains unverified.
- Added capacity-pressure derivation to the Universal Work projector: when a connected commitment/capability quantity falls below a Need, the Need and its local consequences become `at_risk` while canonical status remains unchanged. Added regression for supply 80 against demand 200, including kitchen consequence propagation. API suite passes 35 files / 154 tests and API typecheck/diff check pass.
- Bound capacity truth into the canonical `world-projection` read model: SQL now selects Need/Commitment quantities and actor-capacity values; `buildWorldProjection` carries quantity/unit metadata and derives `derivedState: "at_risk"` for a Need whose explicit Commitment quantity is lower, while preserving lifecycle state. Frontend canonical envelope validation accepts numeric Postgres quantity values. Added API read-model/query regressions; API suite passes 35 files / 156 tests, API typecheck passes, and web suite remains 47 files / 360 tests with typecheck passing.
- Read-only production audit: `https://rizikecosystem.com/api/clink/v1/health` returned 200 with release SHA `d24413e134887af05dae002bce21ccc7e75a34bc`; `/health/ready` returned 503 because database schema reports version 0 versus required 74. Production public smoke passed 3/4; the root rendered an older `C-LINK / PREVIEW` / `You · SUPPLIER` bundle instead of the current local Living Mosaic contract, confirming deployment drift. No mutation or dirty-worktree deployment was attempted. Documented this in `docs/PRODUCTION_READINESS.md` and `docs/operations.md`.
- Added explicit executable evidence for four highest-risk local contracts (`contextual-acceptance`, `cross-actor-sync`, `capacity-consequence`, `temporal-replay`) in `operational-scenario-execution.ts`. All 4 pass as `deterministic_simulation`; the evidence class is deliberately explicit and does not change the 100-scenario catalog's `NOT_RUN` live-runtime status. Targeted API tests pass 7/7 and API typecheck/diff check pass.
- Added `docs/CLINK_MASTER_AUDIT.md`, a requirement-by-requirement evidence ledger separating local proof, deterministic simulation, browser simulation, and missing authenticated production proof. It records current test counts, deployment drift, readiness 503/schema 0, and the exact four-actor runtime sequence still required before completion.
- Expanded the Playwright Universal Work cross-actor regression from Restaurant/Supplier to four simultaneous projections: Restaurant advances the shared append-only event; Supplier becomes blocked with two downstream risks, Farm becomes blocked in both local Works, and Transporter receives the blocked stage while retaining its single active transport Work. Desktop and iPhone profiles both pass (2/2 targeted tests).
- Hardened `scripts/check-web-release.sh`: live HTML must now carry a non-local `clink-release` marker even when asset hashes happen to match. Syntax check passed; current live comparison exits 2 with differing asset hashes, correctly detecting deployment drift.
