# 2026-02-15 Daily Log

## 2D swipe UX fix pass

- Found likely horizontal swipe confusion cause: `RizikScaffold` horizontal `PageController(initialPage: 0)` was not deterministically snapping to center after surfaces load because `controller.page` can be null on first layout.
- Patched deterministic first-center snap using `_didInitialCenterSnap` gate.
- Next: run Flutter macOS and confirm initial surface starts on center feed; verify left/right continuous horizontal swipe across multiple feature screens.

- Center feed fallback colors neutralized to iOS-dark (removed green wash).

- Reel scene UI changed from rounded gradient card to full-bleed TikTok-like overlay on cinematic backdrop.

- Added per-lane page hint: Ops i/N, Finance j/N, Feed.
- Voice AI root-cause #1 fixed: `/live-agent` crashed on macOS with `MissingPluginException` from `permission_handler` in `LiveAgentScreen._connect`.
- Added defensive fallback in `live_agent_screen.dart` to avoid crash when desktop permission plugin channel is unavailable.
- Voice AI root-cause #2 fixed: LiveKit token endpoint hardcoded to `/api/livekit/token`, but deployed web backend uses `/api/token`; added endpoint fallback chain in `livekit_service.dart`.
- Verified in run logs: fallback endpoint used, token received, LiveKit room connected successfully.
