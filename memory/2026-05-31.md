# 2026-05-31

- Investigated Ollama 500 error: `model runner has unexpectedly stopped`.
- Root cause was the default model `gemma4:e2b` crashing during generation on this machine.
- Verified `qwen2.5:0.5b` runs successfully against the same Ollama server.
- Updated `~/.ollama/config.json` to make `qwen2.5:0.5b` the saved last model.
- Ollama server logs are in `~/.ollama/logs/server.log`.
- Fixed `gemma4:e2b` itself by copying a local wrapper with `num_gpu 0` and `num_batch 64`; it now runs without the Metal OOM.
- Updated local OpenClaw config at `~/.openclaw/openclaw.json` to use `ollama/gemma4:e2b-cpu` as the default base model for both defaults and the `main` agent.
