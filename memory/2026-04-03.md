# 2026-04-03

- Checked the actual local inventory first. Active llama.cpp model is `huihui-qwen3.5-4b-claude-4.6-opus-abliterated.q4-k-m` on `http://127.0.0.1:8080/v1`, with a second local llama.cpp model on `8081/v1`.
- Refocused PicoClaw/OpenClaw wiring from MLX to `llamacpp`, so the local Qwen model is now the default base model path instead of the earlier MLX assumption.
- Verification gap: `go`/`gofmt` are not installed in this session, so I could not run Go formatting or test binaries locally.
