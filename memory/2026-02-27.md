# 2026-02-27

## Rizik SaaS - CTO Hardening Pass
- Introduced role-based control-plane guardrails for `/admin` and `/portal`.
- Added public B2B page (`/b2b`) and separated public flow from employee portal access.
- Hardened auth redirect logic with safe `next` handling and role validation.
- Locked `/api/setup-db` to POST-only and added setup-key authorization model.
- Added architecture/security documents for deployment and control-plane governance.
