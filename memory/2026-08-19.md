# 2026-08-19

- Actor Lens browser QA now passes. Opening `Open operating view` intentionally closes the Accessible World Explorer before presenting the actor-scoped queue sheet; keeping both surfaces open caused the Explorer close control to sit under the queue sheet. Closing the queue restores Explorer focus. This preserves one continuous World while avoiding competing action layers. Focused desktop Playwright and web TypeScript check passed.
- Added the second safe half of semantic interaction: `semantic-change-preflight.ts` derives a non-mutating change preflight only for mutable execution records and blocks historical/removed records. Detail now shows `Prepare change` → proposed action + authority/evidence/privacy + explicit “Proposal only” boundary; it can route to the canonical record but cannot mutate shared data. New unit coverage plus focused desktop Playwright passed. Full web suite: 32 files / 313 tests.
- Added backend execution-change proposal ledger foundation: migration `0073_execution_change_proposals.sql` creates immutable workspace-scoped proposals and a one-decision-per-proposal append-only decision table. `/protocol/execution-change-proposals` proposes/reads; `/:id/decision` records approval/rejection. Both validate the subject belongs to the commitment, require active explicit `execution.change.propose` / `execution.change.approve` authority, use idempotency + optimistic commitment version, and append protocol events. Applying an approved proposal remains intentionally separate. API typecheck + 130 API tests; database migration contract (44 tests) passed.
- Added `0074_execution_change_applications.sql` and `/protocol/execution-change-proposals/:id/apply`. An application requires approved decision + distinct `execution.change.apply` authority + correct commitment version; it records an immutable application event. It only supports explicit payloads `assignment.block` and `assignment.cancel`, validates current assignment state and keeps the required-capability status consistent. No arbitrary JSON or responsibility/execution mutation is allowed. API typecheck + 131 API tests; database contract now 45 passing (5 integration skipped).
- Commitment workbench safety integration: assignment actions are now **Propose block** / **Propose cancellation**, not direct `PATCH /assignments/:id` mutation. The operator can add an optional blocking reason, then C-Link posts to the append-only `/protocol/execution-change-proposals` command with the commitment aggregate version. UI explicitly says approval and separate apply are required before any status changes. Web TypeScript + 314 tests passed. Authenticated end-to-end proposal/authority QA is still pending.
- Extended the same workbench into a visible proposal trail: recorded proposals list their privacy/decision state; an explicitly authorized actor can open an approval/rejection review and an approved supported proposal exposes a separate Apply action. New World-owned labels respect workspace Bangla/English. Web TypeScript + 316 tests passed.
- Release-gate correction: API readiness had remained pinned to schema 72 despite migrations 0073/0074 being required by the execution-change routes. It now requires exact migration 74 checksum, the database integration floor is 74, and the v0.1 runbook says 74. API typecheck + 131 tests and DB static migration suite (45 passed, 5 integration tests skipped because no isolated DB URLs) passed. This is source verification only; migrations have not been applied or deployed in this session.
- World renderer loading correction: `PixiWorldSurface` had a static value import for a density constant plus dynamic import for `PixiWorldEngine`, which defeated code splitting. Moved the threshold to lightweight `world-density.ts`; WorldShell now builds as ~321 KB raw and `PixiWorldEngine` as a separate ~428 KB raw lazy chunk (previous WorldShell ~750 KB raw). Added architecture regression; forced web build and 320 web tests passed. Virtual browser QA still cannot launch in this sandbox because Chromium is denied Mach-port registration.
- Asset loading correction: existing C-Link material manifest has ~890 KB critical cork/paper/pin/tape and ~665 KB noncritical yarn/wear, but engine formerly blocked on all 1.5 MB. `PixiWorldEngine` now loads core material for the first usable slice, then upgrades yarn/wear asynchronously without changing scene/camera/focus. Forced build and 321 web tests passed. Visual browser QA remains sandbox-blocked, so this is implementation/build evidence, not new screenshot evidence.

- Dense-world UX pass in `clink-platform`: added `createHighDensityExecutionWorld()` with 53 canonical goods-handoff records, distinct from the 10k renderer benchmarks. At 18+ records it opens as causal branches, and the accessible Explorer shows 40 records then exposes the remainder via `Show 40 more`; desktop and mobile Playwright prove the final evidence record remains reachable.
- The branch explanation now renders from canonical slice density before the GPU snapshot arrives, so first frame has an explicit 53-record branch anchor rather than unexplained empty cork. Material loading remains assistive-technology-visible but no longer places a large generic pill over the World composition. Typecheck, 296 unit tests, focused desktop/mobile Playwright and diff check passed during this pass.
- Legacy visual-ownership audit: all `/clink/world/*` operational routes are WorldShell overlays on the persistent Pixi World. App routes do not register any legacy DOM corkboard (`CorkboardExecutionSlice`, `CorkboardQueue`, `CorkboardReality`, `CorkboardContextSlice`, `CorkboardMockJourney`, `LivingCLinkTrail`) as a production route element; old visual-lab/golden-screen addresses redirect to `/clink/world`. Added an architecture regression. Typecheck, 297 web tests, and diff check passed.
# World UX research / dense-slice correction

- Reviewed 1440×900 goods-handoff (English and Bangla/mixed) screenshots: material hierarchy is in the intended crafted-operational direction; Bangla needs continued wrapping/legibility QA, not a separate visual theme.
- Found a real P0 dense-slice defect: causal cluster card was positioned at its canonical atlas anchor, outside the camera frame that had been composed from compact Slice member positions. The user saw an explanation but no branch to open.
- Corrected `PixiWorldEngine.buildCausalClusterProjection()` so a causal cluster paper derives its position from the centroid of its visible projected canonical members. This does not invent causal grouping; it makes the existing recorded causal branch visible in the current Slice.
- `pnpm --filter @clink/web check` passed; full unit suite passed: 29 files / 298 tests. 1440×900 Playwright visual QA confirms the dense branch paper is now present.

- Refined mobile World UX after explicit slice screenshots: attention is now a compact bottom `Start here` cue in every mobile Slice; Queues/List owns the complete attention inventory. This preserves the physical papers and yarn as orientation rather than covering them with a second queue panel.
- Dense Slice camera now frames a single causal entry cluster as the semantic subject. Collision correction is symmetric for newly created papers, preventing long chains from drifting down/right and pushing the real branch outside the frame.
- Latest checks: TypeScript passed; 29 unit files / 299 tests passed. Mobile Accessible World Explorer regression passed (13 complete test output plus the final unauthenticated-preview test run separately = 14/14).

- Bangla World-operating pass: `PixiWorldSurface` now keeps UI-owned spatial controls and causal/queue labels in the workspace language while preserving canonical record wording as entered truth. `I18nProvider` now applies the initially saved language to `<html lang>`, so Hind Siliguri and assistive-tech language semantics are correct from boot. Desktop Playwright proves `কিউ`, `তালিকা`, Bangla causal explanation/list action and an unchanged `Execution commitment` record in the same journey. Typecheck and 299 unit tests pass.
- Extended that language boundary into the temporal truth surface: World options now exposes Bangla execution-history entry, then Bangla replay/forecast/return controls. Browser test uses the event-bearing goods-handoff fixture (the dense causal fixture correctly has no history events), proving the language layer does not manufacture chronology where the canonical data has none. Full 299 unit tests and two focused desktop language journeys pass.
- Priority-0 guided-UI pass continued: focus inspector's action comprehension labels (state, responsible, decision basis, living execution record, evidence/uncertainty/next action) and queue rationale/authority/evidence/privacy labels are language-aware. Queue domain keys and actual records remain canonical; a local projection maps the seven queue IDs into Bangla presentation. Added focused Bangla desktop detail and 390×844 mobile evidence-capture journeys. Each focused browser journey passed; full unit suite remains 299 tests.
- P1 non-canvas parity: Accessible World Explorer now localizes accessible name, count, Focus/Detail/actor action labels, relationship disclosure and pagination; relationship path/inspector shell localizes direction/permission/provenance without translating edge kind. Focused Bangla Explorer and relationship-inspector browser journeys pass. Deeper provenance explanations and live connection-state labels remain deliberately documented gaps.
- Live freshness safety pass: real-time status in `PixiWorldSurface` now has localized visible text, accessible state description, and a compact stale/offline notice that explicitly asks the operator to recheck the exact record before consequential action. Raw source degradation may still be technical; no fake retry control was added. `LivePixiWorld.temporal.test` locks the safety contract; full suite is now 300 tests. Authenticated socket stale→canonical refresh→recovery remains an explicit unverified QA requirement.
- Provenance language pass: detail/Explorer labels and disclosure boundaries now use workspace language for canonical identity/context, capability/authority/evidence/outcome provenance. `AuthorityProvenanceSummary` receives the language instead of hardcoding English; copied facts stay canonical. Browser detail verifies Bangla identity/context. Source regression locks “recorded provenance is not automatic truth”; 301 tests pass. Fixtures intentionally lack invented provenance-rich data, so full visual provenance QA remains open.
- Replaced that fixture limitation with valid generic protocol data, not decorative fake cards: `createFixtureWorld()` now attaches stable capability, authority, evidence and outcome provenance facts to existing canonical records for goods/service/AI. World-engine test proves each domain uses the same grammar; Bangla browser journey opens evidence, responsibility and outcome details and sees localized shell + canonical authority facts. 1440/390 screenshot density review still remains open.
- UI/UX research synthesis locked: C-Link is a calm operational material World, not photorealistic-paper UI. Default execution cards remain reality/state/relationship papers; sticky/torn/worn treatments are exceptional warning/dispute/artifact states. Updated guided backlog with an actor-relative attention workstream.
- Implemented viewer-relative fixture attention: `WorldScene.viewerActorId` plus explicit `WorldOperationProjection.actionActorIds` prevent other actors' work from being promoted as “Your next action”; live server-provided viewer-relative `assigned` attention is preserved. Added regression coverage; TypeScript and 303 unit tests pass.
- Actual 1440×900 and 390×844 browser QA exposed a mobile Focus issue: the compact inspector could place its only action below docked navigation. Added an action-first compact Focus button; Playwright mobile Focus check now proves `Review commitment` is visible. TypeScript, 303 unit tests, focused mobile Playwright, and diff check passed.
- Actor Lens operational pass: `WorldSpatialState` now carries a stable `lensActorId` (`lensActor` in the URL), so following a queue record cannot accidentally rebind the lens to the selected task. `participantQueuesForScene(scene, actorId)` filters only explicit `operation.actionActorIds`; graph proximity is never treated as an assignment. Opening an actor's operating view opens the queue sheet and names the actor. Typecheck and 305 unit tests pass. Browser actor regression was started but the local GPU-heavy Playwright worker did not return a reliable final transcript; rerun it before claiming browser QA for this exact pass.
- Follow-up live-projection correction: `createLiveWorkspaceWorld()` now attaches `actionActorIds` only to execution/evidence/outcome records that arrived through the server-provided viewer queue projections. It intentionally does not infer scope from `assignedActorId`, actor labels, or a graph edge. Full TypeScript check and all 305 web unit tests pass.
- Direct-manipulation safety pass: added explicit `Arrange this view` mode. It sets only Pixi local-layout drag behavior, displays a local-only/no-semantic-mutation disclosure, and exposes undo/redo/reset. Modifier drag remains supported but is no longer the only entry. Typecheck + 305 web unit tests + focused desktop Playwright test passed.
- Added a two-actor live projection contract: the same commitment can be visible to both actors while A's server queue scopes only execution and B's only evidence. Cross-filtered Actor Lens queues remain empty. Full TypeScript check and 306 web unit tests pass; authenticating two real sessions/realtime recovery remains a required external QA step.
- UI/UX research plan locked in `clink-platform/docs/CLINK_UIUX_OPERATING_MODEL_AND_RESEARCH_PLAN_2026-08-19.md`: C-Link is a spatial execution environment, not a set of corkboard screens. Work is organised around information hierarchy, safe semantic interaction, material trust hierarchy and field/mobile inclusive operation. Current code audit confirms WorldShell/Pixi are the production direction, but focused Pixi World still embeds `CorkboardJourney`; that adapter needs explicit migration/temporary classification before more visual expansion.
- Added `clink-platform/docs/CLINK_WORLD_SCREEN_CONTRACTS_V0_1.md`: exact contracts for World/Slice, Actor Lens, participant Queue, Living Execution Focus, change Workbench, Evidence Field Mode, Timeline and Accessible Explorer. Includes material roles, density rules, explicit safe-change boundary and six cross-domain golden journeys. Next research/build output is a source-by-source existing/partial/missing audit against this contract.
- Completed source-based UI/UX gap matrix and validation protocol in `clink-platform/docs/CLINK_UIUX_CODEBASE_GAP_MATRIX_2026-08-19.md` and `CLINK_WORLD_VALIDATION_PROTOCOL_V0_1.md`. Crucial correction: `EvidenceDrawer` is already a meaningful commitment-scoped mobile capture flow (photo capture, SHA-256/idempotency, local text outbox/draft, reconnect flush, focus trapping). It is partial rather than missing; the correct next work is universal policy-aware upgrade, not a parallel Evidence screen. Added `CLINK_EVIDENCE_CAPTURE_SHEET_SPEC_V0_1.md` for that upgrade, with offline binary privacy/storage boundaries and counterparty handoff rules.
- Evidence Capture Sheet Slice A implemented: `EvidenceDrawer` now accepts an explicit typed canonical subject (with compatibility fallback for older commitment callers), passes subject/obligation/execution scope to record/upload commands, uses subject-scoped local draft/outbox keys, supports optional policy disclosure, and adds a review-before-submit surface that explicitly says submitted evidence is not accepted/verified outcome. `CommitmentActions` now sends an explicit commitment subject instead of relying on implicit drawer subject identity. Added `EvidenceDrawer.contract.test.ts`; web TypeScript and 33 test files / 326 tests pass. No permitted browser screenshot QA yet.
- Evidence Drawer policy integration: Commitment detail now passes canonical required-evidence labels and verification mode to capture; privacy is intentionally not guessed when the projection does not supply it. Tests/typecheck: 33 files / 327 tests passed.
- Migrated focused Pixi World off the legacy `CorkboardJourney` adapter: new `WorldExecutionJourneyRail.tsx` owns the compact data-driven Focus rail and preserves existing World rail CSS, while `CorkboardJourney` is reference/standalone only and returns nothing inside a persistent World. Architecture and temporal tests + TypeScript passed (33 test files / 327 tests). Browser visual verification is still pending due sandbox restrictions.
## C-Link UI/UX — outcome review progression

- Audited the evidence → acceptance → verification boundary in the active C-Link codebase.
- Added outcome-detail provenance retrieval: acceptance assessment, verification assessment, policy/authority IDs, and evidence attached directly to an outcome now return with the commitment record.
- Exposed `verificationPolicyId` through the execution-contract read model.
- Added a policy- and authority-preflighted `Independent verification` panel in `CommitmentActions`: authorized Actors can append `verified`, `not_verified`, or `inconclusive`; unauthorized Actors receive a named handoff instead of a doomed action.
- Added source-contract coverage: `CommitmentOutcomeVerification.contract.test.ts`; extended API outcome-assessment contract test.
- Added `docs/CLINK_OUTCOME_REVIEW_SHEET_SPEC_V0_1.md` documenting the precise UI grammar and open validation gaps.
- Verified: web and API TypeScript checks pass; web 34 files / 332 tests pass; API 31 files / 132 tests pass; `git diff --check` passes.
- Still unproven: authenticated real three-Actor browser/multidevice flow and visual/mobile/a11y screenshot QA, as browser launch is sandbox-blocked.
- Re-ran `pnpm --filter @clink/web qa:world:virtual`; it is correctly blocked by the same macOS sandbox Chromium Mach-port permission (not a product test failure). The harness itself covers desktop 1440×900, handset 390×844, focus/drag/undo, 10k and connected-10k scenes.
- Actor Lens audit found a real mismatch with the UX constitution: it previously traversed two arbitrary active relationship hops, which could imply responsibility from context/proximity. `actorLensNodeIds` now starts only from explicit `actionActorIds` scope and allows one causal supporting hop; tests prove another actor's connected assignment cannot enter the lens. Web typecheck and 34 files / 334 tests pass.
- Visual-system freeze note reviewed and converted into a concrete World rule: a compact paper may show exactly one relationship line, but it must never choose the first adjacent edge by array order. `worldCardSummary` now deterministically prioritizes responsibility → dependency → proof → authority → context, labels the selected family, and retains every other typed edge for the relationship inspector. Added a regression with a context edge deliberately inserted first. Web typecheck and all 34 web test files / 336 tests pass; browser visual QA remains sandbox-blocked.
- Implemented the first direct visual-system consequence: C-Link is now a dedicated **in-path** Pixi marker rather than only a commitment paper. `c-link-path.ts` selects one active participant↔commitment edge per visible commitment using deterministic semantics; the renderer draws `C-LINK · PROPOSED/ACTIVE/EXECUTING/VERIFIED/BLOCKED` above that yarn. Evidence and nonparticipant yarn never receive the marker; no unsupported cancelled state is fabricated. Typecheck and 35 web test files / 339 tests pass, though screenshot/comprehension QA remains pending.
- Formalized the first user-facing semantic projection system instead of adding more pages: `WorldLens` now supports Outcome, Dependency and Proof. `semantic-lenses.ts` derives bounded highlight sets only from active causal relationships; it cannot infer causal meaning from spatial proximity or text. Pixi lens control now exposes these named projections with English/Bangla labels. Typecheck and 36 web test files / 344 tests pass; visual/comprehension validation still remains an explicit gap.
- Counterparty acceptance audit corrected a tempting but unsafe visual idea: public `Recipient.tsx` “Can help”/response is intentionally a capability claim, not commitment acceptance. Alignment acceptance, actor-scoped execution statement, outcome report, outcome acceptance and independent verification are separate share/authority actions. Added `CLINK_COUNTERPARTY_LENS_CONTRACT_V0_1.md` so a future polished `CounterpartyLensSheet` cannot collapse those protocol acts.
- Lens status now shows the localized human projection name rather than raw URL/internal values such as `outcome` or `proof`. Current check: `pnpm --filter @clink/web check`, 36 web test files / 347 tests, and `git diff --check` pass.

- Authenticated live-data World replay completed against the existing signed-in Rizik workspace at `/clink/world`. Accessible Explorer showed 40 permission-visible objects, live queues showed 13 action items / 23 visible queue records, and a real outcome detail showed `200 reported · 198 observed · 198 accepted`, state `partial`, evidence `active`, responsible `Not established`, and next safe action `Track acceptance, partial variance or a dispute`. The same outcome detail remained readable at 390×844 with action-first `Resolve divergence` visible above the mobile dock. Live status correctly surfaced `May be out of date` because `protocol projection` and `workspace claims` could not refresh; no browser console app errors were observed. Web check passed, 36 test files / 350 tests passed, production build passed. `git diff --check` remains blocked by unrelated pre-existing trailing whitespace in backend ghost-engine files.
- Live API tracing isolated the freshness gap: authenticated `GET /protocol/world-projection` returned 500 with correlation IDs `6c3fc9d5-34bf-4638-bc7b-4cc5706eceae` and `06d57fc3-f3e2-4a06-b3b4-ecb3a57270ee`; `GET /protocol/workspace-responsibility-claims` returned 404 because the deployed API route is absent. Public readiness reported the older Worker (`release.sha=unidentified`) and schema 70, while current source requires schema 74. Current local API check, 132 API tests, dry-run build, and web checks/build pass. Tightened `.github/workflows/api-deploy.yml` to require exact schema 74 and verify the four World OpenAPI GET routes after deploy; workflow YAML and scoped diff checks pass. No production deployment was performed.
- Wrangler identity is available with read-only Workers Tail permission, but a filtered tail around a fresh authenticated replay produced no event transcript. Updated `docs/PRODUCTION_READINESS.md` to replace the stale 2026-08-18 database-unavailable claim with the fresh 2026-08-19 evidence: database is reachable under the unidentified schema-70 Worker, World projection is 500, workspace claims route is absent, and no production deployment was made. API/web typechecks and scoped diff check pass.
- Local isolated PostgreSQL is not available (`docker`/`psql` unavailable), so the five database integration tests remain skipped; migration contract coverage is still 45 passing. No remote database mutation or deployment was attempted.
- Deployment gate dry-run against the public API behaves as intended: readiness reports `status=ready` but schema 70 / release `unidentified`, so the exact-schema/SHA gate returns false; the OpenAPI gate also returns false because `/protocol/workspace-responsibility-claims` is missing. Workflow YAML and scoped diff checks remain clean.
- Hardened the OpenAPI deployment gate to read the response from a temporary file instead of passing the full JSON as a shell argument. YAML parse, scoped diff check, and a live contract dry-run (missing claims route → expected failure) pass.
- Extracted the exact API release gate into executable `clink-platform/scripts/check-api-release.sh` and wired `api-deploy.yml` plus the protocol runbook to use it. The reusable gate requires a reviewed `CLINK_RELEASE_SHA`, readiness schema 74/checksum, and all four World GET routes. Against current production it exits 1 with schema 70/unidentified release; missing SHA exits 2. `bash -n`, workflow YAML, and scoped diff checks pass.
- Read-only GitHub Actions audit: latest API run `32260451104` and Web run `32260451320` both failed immediately at `Require deployment credential`; `CLOUDFLARE_API_TOKEN` was empty, so no build, migration, publish, or propagation gate ran. Updated `docs/PRODUCTION_READINESS.md` with this exact external blocker. No GitHub secret or production state was changed.
